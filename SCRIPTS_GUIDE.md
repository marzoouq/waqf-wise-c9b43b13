# 📜 دليل السكريبتات - أدوات الفحص والصيانة

**التاريخ:** 2025-01-16  
**الحالة:** ✅ جاهز للاستخدام

---

## 🎯 السكريبتات المتاحة

### 1. 🏥 Health Check Script
**الملف:** `scripts/health-check.sh`  
**الغرض:** فحص صحة التطبيق بشكل دوري

#### الاستخدام
```bash
# إعطاء صلاحية التنفيذ (مرة واحدة)
chmod +x scripts/health-check.sh

# تشغيل الفحص
./scripts/health-check.sh
```

#### ما يفحصه
- ✅ أخطاء TypeScript
- ✅ مشاكل ESLint
- ✅ console.log في الكود
- ✅ TODO/FIXME/HACK
- ✅ استخدام `any` المفرط
- ✅ حجم Bundle
- ✅ Dependencies قديمة
- ✅ ثغرات أمنية

#### النتائج
```bash
# نجاح كامل
🎉 رائع! التطبيق في حالة ممتازة
✅ 0 أخطاء، 0 تحذيرات

# نجاح مع تحذيرات
⚠️  التطبيق جيد مع بعض التحذيرات
✅ 0 أخطاء، 3 تحذيرات

# فشل
❌ يوجد مشاكل تحتاج إصلاح
❌ 2 أخطاء، 3 تحذيرات
```

---

### 2. 🚀 Pre-Deploy Check Script
**الملف:** `scripts/pre-deploy-check.sh`  
**الغرض:** فحص شامل قبل النشر للإنتاج

#### الاستخدام
```bash
# إعطاء صلاحية التنفيذ (مرة واحدة)
chmod +x scripts/pre-deploy-check.sh

# تشغيل الفحص
./scripts/pre-deploy-check.sh
```

#### ما يفحصه
1. ✅ **TypeScript Compilation** - تجميع TypeScript
2. ✅ **ESLint Check** - فحص الكود
3. ✅ **Production Build** - بناء الإنتاج
4. ✅ **E2E Tests** - اختبارات E2E
5. ✅ **Integration Tests** - اختبارات التكامل
6. ✅ **Security Audit** - فحص أمني
7. ✅ **Bundle Size** - حجم Bundle
8. ✅ **Environment Variables** - متغيرات البيئة
9. ✅ **Supabase Connection** - اتصال Supabase
10. ✅ **PWA Configuration** - إعدادات PWA

#### النتائج
```bash
# جاهز للنشر
┌─────────────────────────────────────┐
│  🎉 جاهز للنشر! جميع الفحوصات نجحت  │
└─────────────────────────────────────┘
✅ يمكنك الآن نشر التطبيق بأمان

# غير جاهز
┌─────────────────────────────────────┐
│  ⚠️  غير جاهز للنشر - يوجد مشاكل    │
└─────────────────────────────────────┘
❌ الرجاء إصلاح المشاكل قبل النشر
```

---

## 🔄 الاستخدام اليومي

### السير العملي الموصى به

#### 1. التطوير اليومي
```bash
# قبل البدء بالعمل
./scripts/health-check.sh

# أثناء التطوير (كل ساعة)
npm run dev

# قبل الـ commit
./scripts/health-check.sh
git add .
git commit -m "وصف التغييرات"
```

#### 2. قبل النشر
```bash
# فحص شامل
./scripts/pre-deploy-check.sh

# إذا نجحت جميع الفحوصات
npm run build
# نشر التطبيق
```

#### 3. فحص دوري (أسبوعي)
```bash
# الإثنين صباحاً
./scripts/health-check.sh

# فحص المكتبات القديمة
npm outdated

# تحديث المكتبات إن لزم
npm update
```

---

## 🛠️ تخصيص السكريبتات

### إضافة فحص جديد

**في `health-check.sh`:**
```bash
# 9. فحص جديد
echo ""
echo "🔍 فحص جديد..."
if [condition]; then
    echo -e "${GREEN}✅ نجح${NC}"
else
    echo -e "${RED}❌ فشل${NC}"
    ERRORS=$((ERRORS + 1))
fi
```

**في `pre-deploy-check.sh`:**
```bash
# 11. فحص جديد
check "اسم الفحص" "الأمر للتنفيذ"
```

---

## 📊 التكامل مع CI/CD

### GitHub Actions Example
```yaml
name: Health Check

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  health-check:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run health check
      run: ./scripts/health-check.sh
    
    - name: Pre-deploy check
      if: github.ref == 'refs/heads/main'
      run: ./scripts/pre-deploy-check.sh
```

### GitLab CI Example
```yaml
stages:
  - test
  - deploy

health-check:
  stage: test
  script:
    - chmod +x scripts/health-check.sh
    - ./scripts/health-check.sh
  only:
    - branches

pre-deploy:
  stage: deploy
  script:
    - chmod +x scripts/pre-deploy-check.sh
    - ./scripts/pre-deploy-check.sh
  only:
    - main
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: Permission Denied
```bash
# الحل
chmod +x scripts/health-check.sh
chmod +x scripts/pre-deploy-check.sh
```

### المشكلة: Command Not Found
```bash
# التأكد من تثبيت Node.js
node --version
npm --version

# تثبيت dependencies
npm install
```

### المشكلة: Build Fails
```bash
# مسح cache
rm -rf node_modules dist
npm install
npm run build
```

### المشكلة: Tests Fail
```bash
# تشغيل الاختبارات يدوياً
npm run test:e2e
npm run test:integration

# فحص السجلات
npm run test -- --verbose
```

---

## 📋 قائمة التحقق اليومية

### صباحاً (قبل البدء)
- [ ] تشغيل `health-check.sh`
- [ ] مراجعة النتائج
- [ ] إصلاح أي مشاكل

### أثناء التطوير
- [ ] فحص دوري كل ساعة
- [ ] commit بعد كل ميزة
- [ ] push في نهاية اليوم

### قبل النهاية
- [ ] تشغيل `health-check.sh`
- [ ] التأكد من عدم وجود أخطاء
- [ ] commit & push

### قبل النشر (أسبوعياً)
- [ ] تشغيل `pre-deploy-check.sh`
- [ ] مراجعة جميع الفحوصات
- [ ] إصلاح أي مشاكل
- [ ] الموافقة على النشر

---

## 📈 المقاييس والمراقبة

### Health Score
```bash
# احسب النتيجة
SCORE = (PASSED / TOTAL) * 100

# تصنيف
100%   = ممتاز ⭐⭐⭐⭐⭐
90-99% = جيد جداً ⭐⭐⭐⭐
80-89% = جيد ⭐⭐⭐
70-79% = مقبول ⭐⭐
<70%   = يحتاج تحسين ⭐
```

### تتبع التقدم
```bash
# سجل النتائج يومياً
echo "$(date): Score $PERCENTAGE%" >> health-log.txt

# عرض الإحصائيات
cat health-log.txt | tail -7  # آخر أسبوع
```

---

## 🎯 أفضل الممارسات

### 1. اجعله عادة
```bash
# أضف alias للـ shell
alias health="./scripts/health-check.sh"
alias predeploy="./scripts/pre-deploy-check.sh"

# استخدم بسهولة
health
predeploy
```

### 2. أتمتة الفحوصات
```bash
# إضافة pre-commit hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/bash
./scripts/health-check.sh
exit $?
EOF

chmod +x .git/hooks/pre-commit
```

### 3. مراقبة مستمرة
```bash
# استخدم watch للفحص المستمر
watch -n 300 ./scripts/health-check.sh  # كل 5 دقائق
```

### 4. توثيق النتائج
```bash
# احفظ النتائج
./scripts/pre-deploy-check.sh > deploy-report-$(date +%Y%m%d).txt
```

---

## 📞 الدعم

### عند وجود مشاكل
1. راجع هذا الدليل
2. افحص السجلات (logs)
3. جرب الحلول المقترحة
4. تواصل مع الفريق

### معلومات التواصل
- **Email:** team@waqf.sa
- **الوثائق:** /docs
- **الدعم الفني:** 24/7

---

## ✅ الخلاصة

**السكريبتات المتاحة:** 2  
**الفحوصات:** 18+ فحص  
**الوقت:** < 5 دقائق لكل فحص  
**الفائدة:** كشف مبكر للمشاكل

**التوصية:** استخدم السكريبتات يومياً لضمان جودة الكود! 🚀

---

**آخر تحديث:** 2025-01-16 09:30 UTC  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للاستخدام ✅
