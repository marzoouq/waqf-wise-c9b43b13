# إعداد البيانات التجريبية للاختبار

## المشكلة الحالية
- واجهة تسجيل الدخول **مرتبطة بالكامل** بقاعدة البيانات
- لكن حسابات المصادقة (auth.users) غير موجودة للمستفيدين
- مما يؤدي لظهور رسالة "رقم الهوية غير مسجل"

## الحل المُنفذ

تم إنشاء Edge Function تقوم بـ:
1. حذف حسابات المصادقة القديمة إن وجدت
2. إنشاء حسابات مصادقة جديدة باستخدام Supabase Admin API
3. ربط الحسابات بسجلات المستفيدين الموجودة
4. إضافة دور "مستفيد" لكل حساب

## تنفيذ الإعداد

### الطريقة 1: استدعاء Edge Function
```bash
curl -X POST \
  https://zsacuvrcohmraoldilph.supabase.co/functions/v1/create-test-beneficiaries \
  -H "Authorization: Bearer YOUR_ANON_KEY"
```

### الطريقة 2: استدعاء من داخل التطبيق
يمكنك استدعاء الدالة من أي صفحة في التطبيق:

```typescript
import { supabase } from '@/integrations/supabase/client';

const setupTestBeneficiaries = async () => {
  const { data, error } = await supabase.functions.invoke('create-test-beneficiaries');
  
  if (error) {
    console.error('خطأ في إنشاء البيانات:', error);
  } else {
    console.log('تم إنشاء البيانات بنجاح:', data);
  }
};
```

## بيانات الاعتماد التجريبية

بعد تشغيل الإعداد، يمكنك تسجيل الدخول باستخدام:

### المستفيدون
| رقم الهوية | الاسم | كلمة المرور |
|------------|-------|-------------|
| 1014548273 | عبدالرحمن مرزوق علي الثبيتي | Test@123456 |
| 1014548257 | فاطمه محمد سعد الشهراني | Test@123456 |
| 1050953866 | امل السيد ابراهيم ابوالريش | Test@123456 |
| 1014548265 | حنان مرزوق علي الثبيتي | Test@123456 |
| 1048839425 | منى مرزوق علي الثبيتي | Test@123456 |

### الموظفون
| البريد الإلكتروني | الدور | كلمة المرور |
|-------------------|-------|-------------|
| nazer@waqf.sa | الناظر | Test@123456 |
| admin@waqf.sa | المشرف | Test@123456 |
| accountant@waqf.sa | المحاسب | Test@123456 |
| cashier@waqf.sa | موظف الصرف | Test@123456 |
| archivist@waqf.sa | أرشيفي | Test@123456 |

## التحقق من نجاح الإعداد

بعد تشغيل الإعداد، تحقق من:

1. **تسجيل الدخول كمستفيد**:
   - افتح صفحة تسجيل الدخول
   - اختر تبويب "المستفيدون"
   - أدخل رقم الهوية: `1014548273`
   - أدخل كلمة المرور: `Test@123456`
   - يجب أن يتم تسجيل الدخول بنجاح

2. **التحقق من البيانات**:
```sql
-- التحقق من المستفيدين
SELECT national_id, full_name, email, user_id, can_login 
FROM beneficiaries 
WHERE national_id IN ('1014548273', '1014548257', '1050953866', '1014548265', '1048839425');

-- التحقق من حسابات المصادقة
SELECT email, created_at 
FROM auth.users 
WHERE email LIKE '%@waqf.internal%';
```

## ملاحظات مهمة

- ⚠️ **البيانات التجريبية**: هذه البيانات للاختبار فقط وليست للإنتاج
- 🔒 **كلمة المرور**: استخدم `Test@123456` لجميع الحسابات التجريبية
- 🔄 **إعادة الإعداد**: يمكنك تشغيل Edge Function مرة أخرى لإعادة إنشاء الحسابات
- ✅ **التأكيد التلقائي**: البريد الإلكتروني مؤكد تلقائياً (email_confirm: true)

## استكشاف الأخطاء

### "رقم الهوية غير مسجل"
- تأكد من تشغيل Edge Function لإنشاء حسابات المصادقة
- تحقق من أن رقم الهوية صحيح (10 أرقام)

### "كلمة المرور غير صحيحة"
- تأكد من استخدام `Test@123456` (حساسة لحالة الأحرف)
- تحقق من عدم وجود مسافات في بداية أو نهاية كلمة المرور

### "حدث خطأ أثناء تسجيل الدخول"
- تحقق من Console للحصول على تفاصيل الخطأ
- تأكد من أن قاعدة البيانات متصلة
- تحقق من إعدادات Supabase Auth
