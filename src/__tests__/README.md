# 📋 منظومة اختبارات منصة إدارة الوقف

## 🗂️ هيكل المجلدات

```
src/__tests__/
├── unit/                    # اختبارات الوحدات
│   ├── services/           # اختبارات الخدمات (70 خدمة)
│   ├── hooks/              # اختبارات الـ Hooks (295+ hook)
│   ├── components/         # اختبارات المكونات (500+ مكون)
│   ├── pages/              # اختبارات الصفحات (79 صفحة)
│   ├── lib/                # اختبارات المكتبات (58 ملف)
│   └── types/              # اختبارات الأنواع (51 ملف)
├── integration/            # اختبارات التكامل
│   ├── auth-flow/          # تدفق المصادقة
│   ├── beneficiary-flow/   # تدفق المستفيدين
│   ├── accounting-flow/    # تدفق المحاسبة
│   ├── property-flow/      # تدفق العقارات
│   ├── distribution-flow/  # تدفق التوزيعات
│   ├── loan-flow/          # تدفق القروض
│   ├── request-flow/       # تدفق الطلبات
│   └── report-flow/        # تدفق التقارير
├── e2e/                    # اختبارات E2E (Playwright)
│   ├── dashboards/         # اختبارات لوحات التحكم
│   ├── pages/              # اختبارات الصفحات
│   ├── workflows/          # اختبارات سير العمل
│   └── mobile/             # اختبارات الموبايل
├── security/               # اختبارات الأمان
│   ├── rls-policies/       # اختبارات سياسات RLS
│   ├── auth-protection/    # اختبارات حماية المصادقة
│   └── data-isolation/     # اختبارات عزل البيانات
├── performance/            # اختبارات الأداء
│   ├── load-times/         # اختبارات زمن التحميل
│   ├── api-response/       # اختبارات استجابة API
│   └── bundle-size/        # اختبارات حجم الـ Bundle
├── database/               # اختبارات قاعدة البيانات
│   ├── tables/             # اختبارات الجداول
│   ├── views/              # اختبارات الـ Views
│   ├── functions/          # اختبارات الدوال
│   └── triggers/           # اختبارات الـ Triggers
└── edge-functions/         # اختبارات Edge Functions
```

## 🧪 أوامر التشغيل

```bash
# تشغيل جميع الاختبارات
npm run test

# تشغيل اختبارات الوحدات فقط
npm run test:unit

# تشغيل اختبارات التكامل
npm run test:integration

# تشغيل اختبارات E2E
npm run test:e2e

# تشغيل اختبارات الأمان
npm run test:security

# تغطية الكود
npm run test:coverage

# مراقبة مستمرة
npm run test:watch
```

## 📊 الإحصائيات المستهدفة

| الفئة | عدد الاختبارات | التغطية |
|-------|---------------|---------|
| الخدمات | ~980 | 100% |
| الـ Hooks | ~2,065 | 100% |
| المكونات | ~3,000 | 100% |
| الصفحات | ~948 | 100% |
| قاعدة البيانات | ~2,188 | 100% |
| Edge Functions | ~210 | 100% |
| التكامل | ~200 | 100% |
| E2E | ~305 | 100% |
| الأمان | ~380 | 100% |
| الأداء | ~169 | 100% |
| **الإجمالي** | **~11,041** | **100%** |

## ✅ معايير الجودة

1. **منع `any`**: لا يُسمح باستخدام `any` في الكود
2. **التغطية الكاملة**: 100% تغطية لجميع الملفات
3. **الاختبارات الحقيقية**: بيانات فعلية من قاعدة البيانات
4. **CI/CD**: الاختبارات تعمل تلقائياً قبل كل merge
5. **Pre-commit hooks**: فحص الكود قبل كل commit
