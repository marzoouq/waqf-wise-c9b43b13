
# خطة إصلاح صفحة المدفوعات `/payments`

## ملخص المشكلة
صفحة المدفوعات تفشل بالتحميل بسبب **49 خطأ TypeScript** في ملفات متعددة. معظم هذه الأخطاء نتجت عن تعديلات سابقة لإصلاح ESLint أدت إلى تغيير الأنواع من محددة إلى `unknown`.

---

## الملفات المتأثرة والإصلاحات المطلوبة

### 1. `src/components/properties/tabs/PaymentsTab.tsx` (11 خطأ)

**المشكلة:** أنواع البيانات غير متوافقة مع `UnifiedDataTable` و `useBulkSelection`

**الإصلاح:**
```typescript
// السطر 135-139: تحديد نوع البيانات في useTableSort
const { sortedData } = useTableSort<RentalPayment>({
  data: filteredPayments,
  defaultSortKey: 'due_date',
  defaultDirection: 'desc',
});

// السطر 148-157: تحديد نوع البيانات في useBulkSelection
const {
  selectedIds,
  selectedCount,
  isSelected,
  isAllSelected,
  toggleSelection,
  toggleAll,
  clearSelection,
} = useBulkSelection<RentalPayment>(paginatedData);

// السطر 280: تحديد نوع UnifiedDataTable
<UnifiedDataTable<RentalPayment>
  title="الدفعات"
  columns={[...]}
  data={paginatedData}
  ...
/>
```

---

### 2. `src/components/properties/tabs/MaintenanceTab.tsx` (9 أخطاء)

**نفس النمط من المشاكل**

**الإصلاح:**
```typescript
// تحديد الأنواع في useTableSort و useBulkSelection
const { sortedData } = useTableSort<MaintenanceRequest>({...});
const { ... } = useBulkSelection<MaintenanceRequest>(paginatedData);

// تحديد نوع UnifiedDataTable
<UnifiedDataTable<MaintenanceRequest>
  ...
/>
```

---

### 3. `src/hooks/ui/useBulkSelection.ts` (تحسين)

**الإصلاح:** التأكد من أن الـ hook يقبل أنواع generic صحيحة
```typescript
export function useBulkSelection<T extends { id: string }>(items: T[]) {
  // ...
}
```

---

### 4. `src/hooks/ui/useTableSort.ts` (تحسين)

**الإصلاح:** التأكد من إرجاع النوع الصحيح
```typescript
export function useTableSort<T>({ data, ... }: { data: T[], ... }): { sortedData: T[] } {
  // ...
}
```

---

### 5. ملفات أخرى تحتاج إصلاح (أخطاء متغيرات)

| الملف | المشكلة | الإصلاح |
|-------|---------|---------|
| `EnhancedAccountsTree.tsx` | `error` غير معرف | استخدام `_error` أو إعادة تسميته |
| `BeneficiariesImporter.tsx` | `error` غير معرف | استخدام `_error` المعرف |
| `BeneficiaryDistributionsTab.tsx` | `isMobile` غير معرف | إضافة `useIsMobile` hook |
| `BeneficiaryStatementsTab.tsx` | `TrendingUp/TrendingDown` غير معرفة | إضافة import من lucide-react |
| `useFiscalYearTests.ts` | `error` غير معرف | استخدام `_error` |
| `useGovernanceBoards.ts` | `boardId` غير معرف | استخدام `_boardId` |
| `useAISystemAudit.ts` | `useMutation` غير معرف | إضافة import |
| `validationSchemas.ts` | `message` غير معرف | استخدام `_message` |
| `ScheduledReportsManager.tsx` | `DialogDescription` غير معرف | إضافة import |
| `SwipeActions.tsx` | متغير مستخدم قبل التعريف | إعادة ترتيب الكود |

---

## خطوات التنفيذ (بالترتيب)

```
┌─────────────────────────────────────────────────────────────┐
│  الخطوة 1: إصلاح الـ Hooks المشتركة                          │
│  📁 useTableSort.ts, useBulkSelection.ts                    │
│  ⏱️ 5 دقائق                                                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  الخطوة 2: إصلاح PaymentsTab.tsx                            │
│  📁 تحديد الأنواع Generic في المكونات                        │
│  ⏱️ 10 دقائق                                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  الخطوة 3: إصلاح MaintenanceTab.tsx                         │
│  📁 نفس نمط الإصلاح                                          │
│  ⏱️ 10 دقائق                                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  الخطوة 4: إصلاح المتغيرات غير المعرفة (10 ملفات)            │
│  📁 إضافة imports المفقودة + تصحيح أسماء المتغيرات           │
│  ⏱️ 15 دقيقة                                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  الخطوة 5: اختبار صفحة /payments                            │
│  ✅ التحقق من التحميل والوظائف                               │
│  ⏱️ 5 دقائق                                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## الوقت التقديري

| المرحلة | الوقت |
|---------|-------|
| إصلاح Hooks | 5 دقائق |
| إصلاح PaymentsTab | 10 دقائق |
| إصلاح MaintenanceTab | 10 دقائق |
| إصلاح المتغيرات (10 ملفات) | 15 دقيقة |
| الاختبار | 5 دقائق |
| **الإجمالي** | **~45 دقيقة** |

---

## ملاحظة مهمة

هذه الأخطاء نتجت عن تعديلات ESLint السابقة التي:
1. غيّرت `as any` إلى `as unknown`
2. أضافت `_` prefix للمتغيرات غير المستخدمة
3. لكن بعض التعديلات أزالت متغيرات بدلاً من تسميتها

**التوصية:** بعد إصلاح هذه الأخطاء، يجب تشغيل `npm run build` للتأكد من عدم وجود أخطاء جديدة.
