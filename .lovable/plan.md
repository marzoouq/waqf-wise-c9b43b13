
# خطة الحماية الشاملة للمستودع - منع التكرار والتضارب

## الوضع الحالي (ما هو موجود)

### ✅ الحمايات الموجودة والفعّالة

| الحماية | الملف | الحالة |
|---------|-------|--------|
| CODEOWNERS | `.github/CODEOWNERS` | ✅ موجود ويحمي 36 ملف/مجلد حرج |
| Pre-commit Hooks | `.husky/pre-commit` | ✅ يفحص TypeScript + ESLint + lint-staged |
| no-duplicate-imports | `eslint.config.js` | ✅ يمنع استيراد نفس الملف مرتين |
| no-explicit-any | `eslint.config.js` | ✅ error - يمنع استخدام any |
| OWNERSHIP_RULES | `docs/OWNERSHIP_RULES.md` | ✅ توثيق قواعد الملكية المعمارية |
| Barrel Exports | 31 ملف `index.ts` | ✅ تصدير مركزي للمكونات |
| Protected Constants | `src/lib/constants.ts` | ✅ 612 سطر من الثوابت الموحدة |
| CI Pipeline | `.github/workflows/ci.yml` | ✅ يفحص TypeScript + ESLint + Build |

### ⚠️ الفجوات الأمنية المكتشفة

| الفجوة | الخطورة | الوصف |
|--------|---------|-------|
| لا توجد حدود استيراد | 🔴 عالية | يمكن لأي ملف استيراد من أي مكان |
| لا يوجد فحص تكرار الكود | 🟠 متوسطة | لا توجد أداة لاكتشاف الكود المكرر |
| لا توجد قوالب PR إجبارية | 🟡 منخفضة | PR Template موجود لكن غير إجباري |
| لا يوجد فحص للملفات المحمية | 🟠 متوسطة | CODEOWNERS يعمل فقط مع GitHub Teams |

---

## الحمايات المطلوب تنفيذها

### 1. حدود الاستيراد (Import Boundaries) - الأهم 🔴

إضافة قواعد ESLint لمنع الاستيرادات غير المصرح بها:

```text
# ملف جديد: .eslintrc.import-rules.json

قواعد الحدود:
├── src/pages/* → لا يستورد من src/pages/* أخرى (منع التبعية الدائرية)
├── src/services/* → لا يستورد من src/hooks/* أو src/components/*
├── src/lib/* → لا يستورد من src/hooks/* أو src/services/*
└── src/hooks/* → يستورد فقط من src/services/* و src/lib/*
```

**الملف المطلوب:** تحديث `eslint.config.js` بإضافة `eslint-plugin-import`

### 2. فحص تكرار الكود (Code Duplication Detection)

**سكريبت جديد:** `scripts/check-code-duplication.js`

```text
الوظائف:
├── فحص الدوال المتشابهة بنسبة > 80%
├── فحص المكونات المتشابهة
├── تحذير إذا وُجد ملفان بنفس الاسم
└── تقرير بالتكرارات المكتشفة
```

### 3. فحص الثوابت المركزية

**سكريبت جديد:** `scripts/check-constants-usage.js`

```text
يفحص:
├── استخدام قيم حرفية بدلاً من الثوابت
├── مثال: 'نشط' بدلاً من BENEFICIARY_STATUS.ACTIVE
├── مثال: 'receipt' بدلاً من PAYMENT_TYPES.RECEIPT
└── يُشغّل في pre-commit
```

### 4. قواعد الاستيراد المقيدة (Restricted Imports)

**إضافة لـ `eslint.config.js`:**

```text
الاستيرادات الممنوعة:
├── supabase مباشرة من المكونات → استخدم Services
├── @tanstack/react-query config → استخدم @/infrastructure/react-query
├── إنشاء QUERY_KEYS محلي → استخدم @/lib/query-keys
└── استخدام matchesStatus محلي → استخدم @/lib/constants
```

### 5. فحص الملفات المحمية

**سكريبت جديد:** `scripts/check-protected-files.js`

```text
الملفات المحمية (تتطلب مراجعة خاصة):
├── src/lib/constants.ts
├── src/infrastructure/react-query/*
├── src/lib/query-keys/*
├── supabase/migrations/*
├── .github/workflows/*
└── docs/ARCHITECTURE*.md
```

---

## خطة التنفيذ

### المرحلة 1: تحديث ESLint (20 دقيقة)

| الملف | التغيير |
|-------|---------|
| `package.json` | إضافة `eslint-plugin-import` |
| `eslint.config.js` | إضافة قواعد no-restricted-imports |

### المرحلة 2: إنشاء سكريبتات الفحص (30 دقيقة)

| الملف | الوظيفة |
|-------|---------|
| `scripts/check-code-duplication.js` | كشف التكرار |
| `scripts/check-constants-usage.js` | فرض استخدام الثوابت |
| `scripts/check-protected-files.js` | حماية الملفات الحرجة |
| `scripts/validate-imports.js` | التحقق من حدود الاستيراد |

### المرحلة 3: تحديث Pre-commit (10 دقيقة)

| الملف | التغيير |
|-------|---------|
| `.husky/pre-commit` | إضافة الفحوصات الجديدة |
| `package.json` | إضافة سكريبتات جديدة |

### المرحلة 4: تحديث CI Pipeline (10 دقيقة)

| الملف | التغيير |
|-------|---------|
| `.github/workflows/ci.yml` | إضافة خطوة فحص التكرار |

---

## الملفات المطلوب إنشاؤها/تعديلها

### ملفات جديدة (4 ملفات)

```text
scripts/check-code-duplication.js     # كشف الكود المكرر
scripts/check-constants-usage.js      # فرض الثوابت المركزية
scripts/check-protected-files.js      # حماية الملفات الحرجة
scripts/validate-imports.js           # التحقق من حدود الاستيراد
```

### ملفات تُعدّل (4 ملفات)

```text
package.json                          # إضافة eslint-plugin-import + سكريبتات
eslint.config.js                      # قواعد الاستيراد المقيدة
.husky/pre-commit                     # إضافة الفحوصات الجديدة
.github/workflows/ci.yml              # إضافة خطوة الفحص
```

---

## قواعد الاستيراد المقيدة (التفاصيل التقنية)

```javascript
// eslint.config.js - الإضافات المطلوبة
"no-restricted-imports": ["error", {
  "patterns": [
    {
      "group": ["@supabase/*"],
      "message": "استخدم @/integrations/supabase/client فقط"
    },
    {
      "group": ["**/lib/queryOptimization*"],
      "message": "deprecated - استخدم @/infrastructure/react-query"
    }
  ],
  "paths": [
    {
      "name": "@tanstack/react-query",
      "importNames": ["QueryClient"],
      "message": "استخدم getQueryClient من @/infrastructure/react-query"
    }
  ]
}]
```

---

## نتيجة التنفيذ المتوقعة

| المقياس | قبل | بعد |
|---------|-----|-----|
| حماية من التكرار | ❌ لا توجد | ✅ فحص تلقائي |
| حماية الثوابت | ⚠️ توثيق فقط | ✅ فحص إجباري |
| حدود الاستيراد | ❌ لا توجد | ✅ ESLint rules |
| حماية الملفات الحرجة | ⚠️ CODEOWNERS فقط | ✅ pre-commit + CI |
| منع supabase مباشر | ❌ لا يوجد | ✅ no-restricted-imports |

---

## الوقت المتوقع للتنفيذ

| المرحلة | الوقت |
|---------|-------|
| تحديث ESLint | 20 دقيقة |
| إنشاء السكريبتات | 30 دقيقة |
| تحديث Pre-commit | 10 دقيقة |
| تحديث CI | 10 دقيقة |
| **الإجمالي** | **~70 دقيقة** |
