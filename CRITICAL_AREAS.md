# 🚨 المناطق الحرجة في النظام | Critical System Areas

> **تحذير شديد الأهمية**: هذا الملف يوثق المناطق الأكثر حساسية في النظام. أي تعديل غير مدروس على هذه الملفات قد يؤدي إلى:
> - 🔒 كسر نظام المصادقة بالكامل
> - 💰 فقدان البيانات المالية
> - 🛡️ ثغرات أمنية خطيرة
> - 👥 فقدان وصول المستخدمين

**قبل أي تعديل على الملفات المدرجة أدناه، يجب:**
1. ✅ قراءة هذا الملف بالكامل
2. ✅ فهم القواعد المحددة لكل منطقة
3. ✅ إجراء الاختبارات المطلوبة
4. ✅ توثيق التغييرات في سجل التغييرات الحرجة

---

## 📋 فهرس المناطق الحرجة | Critical Areas Index

1. [نظام المصادقة](#1-نظام-المصادقة--authentication-system)
2. [مصادقة المستفيدين](#2-مصادقة-المستفيدين--beneficiary-authentication)
3. [سياسات أمان قاعدة البيانات (RLS)](#3-سياسات-أمان-قاعدة-البيانات-rls)
4. [العمليات المالية](#4-العمليات-المالية--financial-operations)
5. [Hooks الحساسة](#5-hooks-الحساسة--sensitive-hooks)

---

## 1. نظام المصادقة | Authentication System

### 🎯 الملفات المحمية | Protected Files

```
src/hooks/useAuth.ts
src/components/auth/ProtectedRoute.tsx
src/pages/Auth.tsx
```

### ⛔ ممنوع منعاً باتاً | STRICTLY FORBIDDEN

- ❌ **تعديل منطق `onAuthStateChange`** - قد يسبب حلقات لا نهائية (deadlock)
- ❌ **إزالة التحقق من الجلسة (session)** - يؤدي لفقدان حالة المصادقة
- ❌ **تعديل `emailRedirectTo`** - يكسر عملية التسجيل بالكامل
- ❌ **استخدام async/await داخل `onAuthStateChange` مباشرة** - يسبب تجميد التطبيق
- ❌ **حذف `updateLastActivity`** - يؤثر على سجلات تتبع المستخدمين

### ✅ التعديلات المسموحة | Allowed Modifications

- ✓ إضافة رسائل خطأ توضيحية
- ✓ تحسين UI للصفحات
- ✓ إضافة تحقق إضافي من المدخلات (Zod validation)
- ✓ تحديث نصوص الواجهة

### 🧪 اختبارات إلزامية قبل Deploy | Required Tests

```bash
# 1. اختبار تسجيل الدخول
- تسجيل دخول مستخدم موجود ✓
- تسجيل دخول بكلمة مرور خاطئة ✓
- تسجيل دخول ببريد غير موجود ✓

# 2. اختبار التسجيل
- تسجيل مستخدم جديد ✓
- التحقق من إنشاء الـ profile تلقائياً ✓
- التحقق من emailRedirectTo ✓

# 3. اختبار الجلسة
- تحديث الصفحة والبقاء مسجل دخول ✓
- تسجيل الخروج ✓
- التحقق من عدم الوصول للصفحات المحمية ✓

# 4. اختبار 2FA (إن كان مفعل)
- تفعيل 2FA ✓
- تسجيل دخول باستخدام 2FA ✓
```

### 🔐 أمثلة على تعديلات خطيرة | Dangerous Modification Examples

```typescript
// ❌ خطير جداً - DON'T DO THIS
supabase.auth.onAuthStateChange(async (event, session) => {
  await supabase.from('profiles').select('*'); // يسبب deadlock!
});

// ✅ الطريقة الصحيحة - DO THIS
supabase.auth.onAuthStateChange((event, session) => {
  setSession(session);
  setUser(session?.user ?? null);
  
  // استخدم setTimeout للعمليات غير المتزامنة
  if (session?.user) {
    setTimeout(() => {
      fetchUserProfile(session.user.id);
    }, 0);
  }
});
```

---

## 2. مصادقة المستفيدين | Beneficiary Authentication

### 🎯 الملفات المحمية | Protected Files

```
src/lib/beneficiaryAuth.ts
```

### ⛔ ممنوع منعاً باتاً | STRICTLY FORBIDDEN

- ❌ **تعديل دالة `nationalIdToEmail`** - تكسر نظام تسجيل دخول المستفيدين بالكامل
- ❌ **تغيير النطاق `@waqf.internal`** - يؤدي لفقدان وصول جميع المستفيدين
- ❌ **تعديل regex التحقق من الهوية** - قد يسمح بأرقام هوية غير صحيحة
- ❌ **تغيير صيغة كلمة المرور المؤقتة** - يكسر تسجيل الدخول للمستفيدين الحاليين

### ✅ التعديلات المسموحة | Allowed Modifications

- ✓ إضافة دوال تحقق إضافية (مع الحفاظ على الموجودة)
- ✓ إضافة logging للعمليات
- ✓ تحسين رسائل الخطأ

### 🧪 اختبارات إلزامية | Required Tests

```bash
# اختبار شامل
- التحقق من صحة رقم هوية (10 أرقام) ✓
- تحويل رقم هوية إلى email ✓
- استخراج رقم هوية من email ✓
- رفض أرقام هوية غير صحيحة ✓
- تسجيل دخول مستفيد حقيقي ✓
```

### 📊 إحصائيات الاستخدام | Usage Statistics

```
عدد المستفيدين المسجلين: يتم حسابه من جدول beneficiaries
نسبة نجاح تسجيل الدخول: يجب أن تكون > 95%
```

---

## 3. سياسات أمان قاعدة البيانات (RLS)

### 🎯 الملفات المحمية | Protected Files

```
supabase/migrations/*.sql (جميع ملفات الـ migrations)
```

### ⛔ ممنوع منعاً باتاً | STRICTLY FORBIDDEN

- ❌ **حذف أو تعطيل RLS على الجداول الحساسة** (beneficiaries, payments, distributions)
- ❌ **إنشاء سياسة `FOR ALL TO public`** بدون شروط - ثغرة أمنية خطيرة
- ❌ **استخدام استعلامات داخل RLS تشير لنفس الجدول** - يسبب infinite recursion
- ❌ **السماح بوصول غير مصرح به للبيانات الشخصية** (PII)

### ✅ التعديلات المسموحة | Allowed Modifications

- ✓ إنشاء سياسات جديدة بشروط صارمة
- ✓ استخدام SECURITY DEFINER functions
- ✓ إضافة فحوصات إضافية للأمان

### 🔒 معايير الأمان الإلزامية | Security Standards

```sql
-- ✅ مثال صحيح - استخدام security definer function
CREATE POLICY "Users can view own beneficiaries"
ON beneficiaries FOR SELECT
USING (public.has_role(auth.uid(), 'nazer') OR user_id = auth.uid());

-- ❌ خطأ شائع - استعلام داخلي يسبب recursion
CREATE POLICY "Admins can view all"
ON profiles FOR SELECT
USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');
```

### 🧪 اختبارات الأمان | Security Tests

```bash
# يجب اختبار كل سياسة:
1. محاولة الوصول بمستخدم غير مصرح ✗
2. الوصول بمستخدم مصرح ✓
3. محاولة تعديل بيانات مستخدم آخر ✗
4. محاولة حذف بيانات بدون صلاحية ✗
```

### 📋 الجداول الأكثر حساسية | Most Sensitive Tables

| الجدول | مستوى الحساسية | RLS مطلوب |
|--------|----------------|-----------|
| `beneficiaries` | 🔴 حرج جداً | ✅ نعم |
| `payments` | 🔴 حرج جداً | ✅ نعم |
| `distributions` | 🔴 حرج جداً | ✅ نعم |
| `journal_entries` | 🟠 حرج | ✅ نعم |
| `bank_accounts` | 🟠 حرج | ✅ نعم |
| `user_roles` | 🔴 حرج جداً | ✅ نعم |

---

## 4. العمليات المالية | Financial Operations

### 🎯 المكونات المحمية | Protected Components

```
src/pages/Funds.tsx
src/components/funds/*
src/hooks/useDistributions.ts
```

### ⛔ ممنوع منعاً باتاً | STRICTLY FORBIDDEN

- ❌ **تعديل حسابات التوزيعات بدون مراجعة محاسبية**
- ❌ **السماح بإنشاء توزيع بدون موافقات**
- ❌ **تجاوز مسار الموافقات (Approval Workflow)**
- ❌ **حذف سجلات مالية (Audit Trail)**

### ✅ التعديلات المسموحة | Allowed Modifications

- ✓ تحسين واجهة المستخدم
- ✓ إضافة تقارير جديدة
- ✓ تحسين رسائل الخطأ والتحقق من المدخلات

### 💰 قواعد العمليات المالية | Financial Operation Rules

1. **كل توزيع مالي يجب أن:**
   - يمر بمسار الموافقات الكامل
   - يُسجل في سجل العمليات (audit_logs)
   - يُنشئ قيد محاسبي تلقائي
   - يُرسل إشعارات للمستفيدين

2. **لا يمكن حذف أو تعديل:**
   - توزيعات منفذة (executed distributions)
   - قيود محاسبية معتمدة
   - سجلات الدفع المكتملة

### 🧪 اختبارات العمليات المالية | Financial Tests

```bash
# قبل أي تعديل:
1. إنشاء توزيع جديد ✓
2. التحقق من حساب المبالغ صحيح ✓
3. مراجعة المحاسب ✓
4. موافقة الناظر ✓
5. إنشاء قيد محاسبي تلقائي ✓
6. التحقق من الإشعارات للمستفيدين ✓
7. التحقق من سجل العمليات ✓
```

---

## 5. Hooks الحساسة | Sensitive Hooks

### 🎯 الملفات المحمية | Protected Files

```
src/hooks/useAuth.ts
src/hooks/useUserRole.ts
src/hooks/useApprovals.ts
src/hooks/useDashboardKPIs.ts
src/hooks/useFinancialData.ts
```

### ⛔ ممنوع منعاً باتاً | STRICTLY FORBIDDEN

- ❌ **تعديل منطق الـ caching بدون فهم React Query**
- ❌ **حذف التحقق من الصلاحيات في useUserRole**
- ❌ **تجاوز القراءة من الـ session الفعلية**
- ❌ **إزالة error handling من العمليات الحرجة**

### ✅ التعديلات المسموحة | Allowed Modifications

- ✓ تحسين أداء الـ queries
- ✓ إضافة مزيد من الـ error handling
- ✓ تحديث staleTime و cacheTime بحذر

### 🧪 اختبارات Hooks | Hook Tests

```bash
# اختبار useUserRole:
- التحقق من تحميل الأدوار بشكل صحيح ✓
- اختبار hasRole() لكل دور ✓
- التحقق من real-time updates ✓

# اختبار useAuth:
- التحقق من حالة loading ✓
- اختبار signIn/signOut ✓
- التحقق من session persistence ✓

# اختبار useDashboardKPIs:
- دقة الحسابات المالية ✓
- أداء الـ queries ✓
- التحقق من التحديثات real-time ✓
```

---

## 📝 سجل التغييرات الحرجة | Critical Changes Log

### كيفية استخدام السجل | How to Use

قبل أي تعديل على المناطق الحرجة، أضف سجلاً هنا:

```markdown
### [التاريخ] - [اسم المطور]

**الملف المعدل:** `مسار الملف`

**سبب التعديل:**
وصف مختصر للسبب

**التغييرات:**
- ما تم تعديله بالتحديد
- ما تم إضافته
- ما تم حذفه

**الاختبارات المنفذة:**
- [ ] الاختبار 1
- [ ] الاختبار 2

**المراجع:**
- الناظر: تمت المراجعة ✓ / لا ✗
- المحاسب: تمت المراجعة ✓ / لا ✗

**النتيجة:**
- ✅ نجح التعديل
- ❌ فشل (مع شرح السبب)
```

---

## 🆘 ماذا تفعل إذا انكسر شيء | What to Do If Something Breaks

### 1. **نظام المصادقة لا يعمل** 🔒

```bash
# الخطوات الفورية:
1. تحقق من console.log في useAuth.ts
2. تحقق من Auth logs في Supabase
3. تحقق من emailRedirectTo في Auth.tsx
4. استخدم git revert للعودة للنسخة السابقة
```

### 2. **المستفيدون لا يستطيعون تسجيل الدخول** 👥

```bash
# الخطوات الفورية:
1. تحقق من beneficiaryAuth.ts لم يتم تعديله
2. تحقق من auth.users في Supabase
3. تحقق من RLS policies على جدول beneficiaries
4. تحقق من الـ domain @waqf.internal لم يتغير
```

### 3. **العمليات المالية لا تعمل** 💰

```bash
# الخطوات الفورية:
1. لا تحاول إصلاحه بنفسك - اتصل بالمحاسب!
2. تحقق من audit_logs لآخر عملية ناجحة
3. تحقق من RLS policies على جداول المال
4. استخدم git revert فوراً
5. راجع مع الناظر قبل أي محاولة إصلاح
```

### 4. **RLS Policies تسبب أخطاء** 🛡️

```bash
# الخطوات الفورية:
1. تحقق من Supabase logs
2. ابحث عن "infinite recursion" أو "policy violation"
3. راجع SECURITY.md
4. استخدم security definer functions
5. لا تعطل RLS - أصلح السياسات
```

---

## 👤 جهات الاتصال للطوارئ | Emergency Contacts

### للمراجعة الأمنية | Security Review
- **الدور:** مدير النظام / Admin
- **التواصل:** عبر لوحة التحكم

### للمراجعة المالية | Financial Review
- **الدور:** الناظر / المحاسب
- **التواصل:** يجب موافقتهم على أي تعديل مالي

### للدعم التقني | Technical Support
- **المرجع:** SECURITY.md و README.md
- **الوثائق:** [Lovable Docs](https://docs.lovable.dev)

---

## 🎓 قواعد عامة للمطورين | General Developer Rules

### ✅ افعل | DO

1. ✅ اقرأ هذا الملف قبل تعديل أي ملف محمي
2. ✅ اختبر كل تعديل بشكل شامل
3. ✅ وثّق التغييرات في سجل التغييرات
4. ✅ استخدم git branches للتعديلات الحرجة
5. ✅ اطلب المراجعة قبل merge

### ❌ لا تفعل | DON'T

1. ❌ لا تعدل الملفات الحرجة بدون فهم كامل
2. ❌ لا تتجاوز اختبارات الأمان
3. ❌ لا تحذف RLS policies
4. ❌ لا تعطل سجل العمليات (Audit Logs)
5. ❌ لا تنشر (deploy) تعديلات غير مختبرة

---

## 📚 مراجع إضافية | Additional References

- [SECURITY.md](./SECURITY.md) - السياسات الأمنية الكاملة
- [Supabase RLS Docs](https://supabase.com/docs/guides/auth/row-level-security)
- [React Query Best Practices](https://tanstack.com/query/latest/docs/react/guides/important-defaults)

---

**آخر تحديث:** 2025-11-23

**رقم الإصدار:** 1.0.0

---

> 💡 **ملاحظة مهمة:** هذا الملف يجب أن يُحدث عند:
> - إضافة مناطق حرجة جديدة
> - اكتشاف ثغرات أمنية
> - تعديل معايير الأمان
> - تغيير في البنية التحتية الحرجة
