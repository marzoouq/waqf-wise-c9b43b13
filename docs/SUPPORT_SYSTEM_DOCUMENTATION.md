# 📨 نظام الدعم الفني - التوثيق الشامل

## نظرة عامة

تم تطوير نظام دعم فني متكامل يربط المستفيدين بفريق الدعم عبر نظام تذاكر احترافي مع إمكانيات تتبع وإدارة شاملة.

---

## 🎯 الأهداف الرئيسية

1. **تسهيل التواصل**: توفير قناة واضحة ومباشرة بين المستفيدين وفريق الدعم
2. **تتبع الطلبات**: نظام تذاكر منظم مع أرقام فريدة لكل طلب
3. **الاستجابة السريعة**: آليات لتحديد الأولويات وتعيين المسؤولين
4. **الشفافية**: إمكانية متابعة حالة الطلب في أي وقت
5. **القياس والتحسين**: إحصائيات وتقارير لتحسين جودة الخدمة

---

## 🏗️ المكونات الأساسية

### 1. واجهة المستفيد (`/beneficiary-support`)

**الملف**: `src/pages/BeneficiarySupport.tsx`

#### الميزات:
- **تبويب الأسئلة الشائعة (FAQ)**:
  - 8 أسئلة شائعة مع إجابات تفصيلية
  - نظام Accordion قابل للطي والفتح
  - تغطي جميع الاستفسارات الشائعة

- **تبويب مراسلة الدعم**:
  - نموذج إرسال رسالة بسيط
  - حقول: الموضوع والرسالة
  - زر إرسال مع حالة تحميل

- **معلومات التواصل السريع**:
  ```
  📞 الهاتف: 0533030345 (قابل للنقر للاتصال المباشر)
  📧 البريد الإلكتروني: support@waqf.sa
  ```

#### التكامل:
```typescript
import { useSupportTickets } from "@/hooks/useSupportTickets";

const { createTicket } = useSupportTickets();

// إنشاء تذكرة جديدة
await createTicket.mutateAsync({
  subject: "موضوع الرسالة",
  description: "نص الرسالة",
  category: 'inquiry',
  priority: 'medium',
  beneficiary_id: beneficiary?.id,
});
```

---

### 2. واجهة إدارة الدعم (`/support-management`)

**الملف**: `src/pages/SupportManagement.tsx`

#### التبويبات:

##### أ. نظرة عامة (Overview)
- **بطاقات الإحصائيات**:
  - التذاكر المفتوحة
  - التذاكر قيد المعالجة
  - التذاكر المحلولة اليوم
  - متوسط وقت الاستجابة

- **التذاكر المتأخرة**: قائمة بالتذاكر التي تجاوزت SLA
- **التذاكر الأخيرة**: آخر 10 تذاكر مع إمكانية الفتح المباشر

##### ب. جميع التذاكر
- عرض جدولي شامل لكل التذاكر
- فلاتر متقدمة:
  - الحالة (مفتوحة، قيد المعالجة، محلولة، مغلقة)
  - التصنيف
  - الأولوية
  - المسؤول
  - نطاق التاريخ
  - البحث النصي

##### ج. التحليلات (Analytics)
- رسوم بيانية للأداء
- معدلات الحل
- توزيع التذاكر حسب التصنيف

##### د. أداء الموظفين
- تقارير أداء فردية لكل موظف دعم
- عدد التذاكر المعالجة
- متوسط وقت الاستجابة
- تقييمات العملاء

---

### 3. نظام التذاكر (Support Tickets)

#### هيكل التذكرة:

```typescript
interface SupportTicket {
  id: string;                    // UUID فريد
  ticket_number: string;         // رقم تذكرة قابل للقراءة (مثل: TKT-2025-001)
  beneficiary_id: string;        // معرف المستفيد
  subject: string;               // موضوع الرسالة
  description: string;           // نص الرسالة التفصيلي
  category: TicketCategory;      // التصنيف
  priority: TicketPriority;      // الأولوية
  status: TicketStatus;          // الحالة
  assigned_to: string | null;   // معرف الموظف المسؤول
  created_at: string;            // تاريخ الإنشاء
  first_response_at: string;     // تاريخ أول رد
  resolved_at: string;           // تاريخ الحل
  sla_due_at: string;            // الموعد النهائي للرد حسب SLA
  is_overdue: boolean;           // هل تجاوز الموعد؟
  response_count: number;        // عدد الردود
}
```

#### التصنيفات (Categories):
- `technical`: مشاكل تقنية
- `financial`: استفسارات مالية
- `account`: حسابات المستخدمين
- `request`: طلبات عامة
- `complaint`: شكاوى
- `inquiry`: استفسارات (الافتراضي للمستفيدين)
- `other`: أخرى

#### الأولويات (Priorities):
- `low`: منخفضة
- `medium`: متوسطة (الافتراضي)
- `high`: عالية
- `urgent`: عاجلة

#### الحالات (Status):
- `open`: مفتوحة (جديدة)
- `in_progress`: قيد المعالجة
- `waiting_customer`: بانتظار رد المستفيد
- `resolved`: تم الحل
- `closed`: مغلقة
- `cancelled`: ملغية

---

## 🔄 سير العمل (Workflow)

### 1. إنشاء التذكرة
```
المستفيد → نموذج الدعم → createTicket → قاعدة البيانات
                                      ↓
                              رقم تذكرة تلقائي
                                      ↓
                              إشعار للفريق
```

### 2. معالجة التذكرة
```
فريق الدعم → استلام التذكرة → مراجعة
                          ↓
                    تعيين مسؤول
                          ↓
                    تغيير الحالة إلى "قيد المعالجة"
                          ↓
                    إضافة تعليقات/ردود
                          ↓
                    تحديث الحالة إلى "محلولة"
```

### 3. إغلاق التذكرة
```
فريق الدعم → تأكيد الحل → تغيير إلى "مغلقة"
                          ↓
                    طلب تقييم من المستفيد
```

---

## 🔗 APIs والوظائف

### Hook: `useSupportTickets`

**الموقع**: `src/hooks/useSupportTickets.ts`

#### الوظائف المتاحة:

##### 1. جلب التذاكر مع فلاتر
```typescript
const { tickets, isLoading } = useSupportTickets({
  status: ['open', 'in_progress'],
  category: ['inquiry'],
  priority: ['high', 'urgent'],
  assigned_to: userId,
  search: "نص البحث",
  date_from: "2025-01-01",
  date_to: "2025-12-31",
  is_overdue: true
});
```

##### 2. إنشاء تذكرة جديدة
```typescript
const { createTicket } = useSupportTickets();

await createTicket.mutateAsync({
  subject: "موضوع التذكرة",
  description: "الوصف التفصيلي",
  category: 'inquiry',
  priority: 'medium',
  beneficiary_id: 'uuid-here'
});
```

##### 3. تحديث التذكرة
```typescript
const { updateTicket } = useSupportTickets();

await updateTicket.mutateAsync({
  id: ticketId,
  status: 'in_progress',
  assigned_to: userId
});
```

##### 4. إغلاق التذكرة
```typescript
const { closeTicket } = useSupportTickets();

await closeTicket.mutate(ticketId);
```

##### 5. إعادة فتح التذكرة
```typescript
const { reopenTicket } = useSupportTickets();

await reopenTicket.mutate(ticketId);
```

##### 6. تعيين التذكرة لموظف
```typescript
const { assignTicket } = useSupportTickets();

await assignTicket.mutateAsync({
  ticketId: ticketId,
  userId: assigneeId
});
```

---

## 📊 الإحصائيات والتقارير

### Hook: `useSupportStats`

```typescript
const { 
  overviewStats,      // إحصائيات عامة
  overdueTickets,     // التذاكر المتأخرة
  recentTickets,      // آخر التذاكر
  overviewLoading     // حالة التحميل
} = useSupportStats();
```

#### البيانات المتوفرة:
- عدد التذاكر المفتوحة
- عدد التذاكر قيد المعالجة
- عدد التذاكر المحلولة
- متوسط وقت الاستجابة الأولى
- متوسط وقت الحل
- معدل الالتزام بـ SLA
- متوسط التقييمات

---

## 🔔 نظام الإشعارات

### متى يتم إرسال الإشعارات؟

1. **عند إنشاء تذكرة جديدة**:
   - إشعار لجميع موظفي الدعم المتاحين
   - إشعار للمشرف (Supervisor)

2. **عند تعيين تذكرة**:
   - إشعار للموظف المُعيّن

3. **عند الرد على التذكرة**:
   - إشعار للمستفيد (إذا كان الرد من الفريق)
   - إشعار للموظف المُعيّن (إذا كان الرد من المستفيد)

4. **عند تجاوز SLA**:
   - إشعار تصعيدي للمشرف
   - تحديد التذكرة كمتأخرة

5. **عند حل التذكرة**:
   - إشعار للمستفيد مع طلب تقييم

---

## 🔐 الصلاحيات والأمان

### RLS Policies

#### جدول `support_tickets`:

```sql
-- المستفيدون: يمكنهم فقط رؤية تذاكرهم الخاصة
CREATE POLICY "beneficiaries_select_own_tickets"
ON support_tickets FOR SELECT
USING (
  auth.uid() IN (
    SELECT user_id FROM beneficiaries WHERE id = support_tickets.beneficiary_id
  )
);

-- المستفيدون: يمكنهم إنشاء تذاكر جديدة
CREATE POLICY "beneficiaries_insert_tickets"
ON support_tickets FOR INSERT
WITH CHECK (
  auth.uid() IN (
    SELECT user_id FROM beneficiaries WHERE id = beneficiary_id
  )
);

-- فريق الدعم: يمكنهم رؤية جميع التذاكر
CREATE POLICY "support_staff_all_access"
ON support_tickets FOR ALL
USING (
  EXISTS (
    SELECT 1 FROM user_roles
    WHERE user_id = auth.uid()
    AND role IN ('admin', 'nazer', 'accountant')
  )
);
```

---

## 📱 التكامل مع الواجهات الأخرى

### 1. لوحة تحكم المستفيد
- رابط سريع في القائمة الرئيسية
- إشعارات للردود الجديدة
- عداد للتذاكر المفتوحة

### 2. نظام الرسائل الداخلية
- التكامل متاح لإرسال رسائل مباشرة للدعم
- يمكن ربط الرسائل بالتذاكر

### 3. لوحة تحكم الناظر
- عرض إحصائيات سريعة للتذاكر
- تنبيهات للتذاكر العاجلة
- تقارير الأداء الشهرية

---

## 🧪 الاختبارات

### ملف الاختبار: `src/__tests__/e2e/support/support-tickets.spec.ts`

#### السيناريوهات المختبرة:

1. **إنشاء تذكرة دعم - مستفيد**
   - تسجيل دخول كمستفيد
   - ملء نموذج التذكرة
   - التحقق من الإرسال الناجح

2. **إدارة التذاكر - فريق الدعم**
   - عرض قائمة التذاكر
   - التحقق من وجود البيانات

3. **الرد على تذكرة**
   - فتح تذكرة
   - إضافة رد
   - التحقق من الحفظ

4. **إغلاق تذكرة**
   - تحديد تذكرة مفتوحة
   - تغيير الحالة إلى مغلقة
   - التحقق من التحديث

---

## 🐛 الإصلاحات التي تمت

### 1. إصلاح خطأ UUID في `request_type_id`
**المشكلة**: كان الكود يرسل `"support"` كنص بدلاً من UUID أو null.

**الحل**:
```typescript
// قبل الإصلاح ❌
request_type_id: "support"

// بعد الإصلاح ✅
// استخدام نظام التذاكر بدلاً من beneficiary_requests
```

### 2. التحويل من `beneficiary_requests` إلى `support_tickets`
**التحسين**: نقل المنطق من جدول الطلبات العامة إلى نظام التذاكر المتخصص.

**الفوائد**:
- نظام موحد للدعم الفني
- إمكانيات تتبع أفضل
- تقارير وإحصائيات دقيقة
- تكامل مع نظام SLA

### 3. إضافة رقم الهاتف للحالات العاجلة
**الإضافة**: رقم 0533030345 كرابط قابل للنقر.

```tsx
<a href="tel:+966533030345" className="...">
  0533030345
</a>
```

---

## 📈 المقاييس والأهداف (KPIs)

### مؤشرات الأداء الرئيسية:

1. **متوسط وقت الاستجابة الأولى**: < 2 ساعة
2. **متوسط وقت الحل**: < 24 ساعة
3. **نسبة الالتزام بـ SLA**: > 95%
4. **رضا العملاء**: > 4.5/5
5. **نسبة التذاكر المعادة**: < 5%

### تتبع الأداء:
```typescript
const stats = await supabase
  .from('support_statistics')
  .select('*')
  .eq('date', today);
```

---

## 🔮 التطويرات المستقبلية

### على المدى القريب:
- [ ] إضافة دردشة مباشرة (Live Chat)
- [ ] تحميل مرفقات للتذاكر
- [ ] نظام تقييم للردود
- [ ] قوالب للردود السريعة

### على المدى المتوسط:
- [ ] روبوت دردشة AI للأسئلة الشائعة
- [ ] تحليل مشاعر التذاكر
- [ ] توصيات تلقائية للحلول
- [ ] دعم متعدد اللغات

### على المدى البعيد:
- [ ] تكامل مع WhatsApp Business
- [ ] نظام معرفة قاعدة Knowledge Base متقدم
- [ ] تحليلات تنبؤية للأحمال
- [ ] أتمتة كاملة للردود البسيطة

---

## 📚 المراجع والموارد

### الملفات الرئيسية:
```
src/
├── pages/
│   ├── BeneficiarySupport.tsx        # واجهة المستفيد
│   ├── SupportManagement.tsx         # واجهة الإدارة
│   └── Support.tsx                   # واجهة الدعم العامة
├── hooks/
│   ├── useSupportTickets.ts          # إدارة التذاكر
│   ├── useSupportStats.ts            # الإحصائيات
│   └── useTicketComments.ts          # التعليقات
├── components/
│   └── support/
│       ├── CreateTicketDialog.tsx    # نموذج إنشاء تذكرة
│       ├── TicketDetailsDialog.tsx   # تفاصيل التذكرة
│       └── TicketRatingDialog.tsx    # تقييم الخدمة
├── types/
│   └── support.ts                    # أنواع TypeScript
└── __tests__/
    └── e2e/
        └── support/
            └── support-tickets.spec.ts  # الاختبارات

supabase/
└── functions/
    └── support-auto-escalate/
        └── index.ts                   # دالة التصعيد التلقائي
```

### الروابط المفيدة:
- [Supabase Documentation](https://supabase.com/docs)
- [React Query Documentation](https://tanstack.com/query/latest)
- [Lovable Documentation](https://docs.lovable.dev/)

---

## 📞 الدعم والاتصال

للاستفسارات التقنية أو المساعدة:
- **الهاتف**: 0533030345
- **البريد**: support@waqf.sa
- **داخل النظام**: استخدم نموذج الدعم المدمج

---

## 📝 سجل التغييرات

### النسخة 1.0.0 (2025-01-26)
- ✅ إطلاق نظام التذاكر الأساسي
- ✅ واجهة المستفيد مع الأسئلة الشائعة
- ✅ واجهة إدارة الدعم
- ✅ نظام الإحصائيات والتقارير
- ✅ إضافة رقم الهاتف للحالات العاجلة
- ✅ إصلاح خطأ إرسال التذاكر

---

**آخر تحديث**: 26 يناير 2025  
**الإصدار**: 1.0.0  
**الحالة**: مفعّل ✅
