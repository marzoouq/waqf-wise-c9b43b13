# 🏗️ هيكل مشروع منصة إدارة الوقف

## 📁 نظرة عامة على المجلدات

```
src/
├── __tests__/           # اختبارات الوحدات والتكامل والـ E2E
├── assets/              # الصور والأيقونات
├── components/          # مكونات React القابلة لإعادة الاستخدام
├── contexts/            # سياقات React (Auth, Theme, etc.)
├── hooks/               # Custom Hooks
├── integrations/        # تكاملات خارجية (Supabase)
├── lib/                 # مكتبات ودوال مساعدة
├── pages/               # صفحات التطبيق
├── services/            # خدمات الأعمال
├── types/               # تعريفات TypeScript
└── test/                # إعدادات الاختبار
```

---

## 📂 تفصيل المجلدات

### `/src/components/` - المكونات

| المجلد | الوصف |
|--------|-------|
| `accounting/` | مكونات المحاسبة (القيود، الحسابات، التقارير) |
| `admin/` | مكونات لوحة الإدارة |
| `archive/` | مكونات الأرشفة الإلكترونية |
| `auth/` | مكونات المصادقة (Login, Signup, Guards) |
| `beneficiaries/` | مكونات إدارة المستفيدين |
| `beneficiary/` | مكونات بوابة المستفيد |
| `dashboard/` | مكونات لوحات التحكم |
| `developer/` | أدوات المطور |
| `distributions/` | مكونات التوزيعات |
| `documents/` | مكونات المستندات |
| `families/` | مكونات إدارة العائلات |
| `funds/` | مكونات إدارة الصناديق |
| `governance/` | مكونات الحوكمة |
| `loans/` | مكونات القروض |
| `messages/` | مكونات الرسائل الداخلية |
| `notifications/` | مكونات الإشعارات |
| `properties/` | مكونات إدارة العقارات |
| `reports/` | مكونات التقارير |
| `requests/` | مكونات الطلبات |
| `settings/` | مكونات الإعدادات |
| `support/` | مكونات الدعم الفني |
| `system/` | مكونات النظام |
| `ui/` | مكونات Shadcn UI |

### `/src/hooks/` - الـ Custom Hooks

| الفئة | الملفات | الوصف |
|-------|---------|-------|
| **المصادقة** | `useAuth`, `useUserRole`, `usePermissions` | إدارة المصادقة والصلاحيات |
| **المستفيدين** | `useBeneficiaries`, `useBeneficiaryProfile`, `useFamilies` | إدارة بيانات المستفيدين |
| **المالية** | `useAccounts`, `useJournalEntries`, `useBudgets` | العمليات المحاسبية |
| **التوزيعات** | `useDistributions`, `useDistributionEngine` | إدارة التوزيعات |
| **العقارات** | `useProperties`, `useContracts`, `useMaintenanceRequests` | إدارة العقارات |
| **الطلبات** | `useRequests`, `useApprovals`, `useApprovalWorkflows` | إدارة الطلبات والموافقات |
| **الإشعارات** | `useNotifications`, `useNotificationSystem` | نظام الإشعارات |
| **الأدوات** | `useLocalStorage`, `useDebounce`, `useVirtualization` | أدوات مساعدة |

### `/src/services/` - الخدمات

| الخدمة | الملف | الوصف |
|--------|-------|-------|
| المستفيدين | `beneficiary.service.ts` | عمليات CRUD للمستفيدين |
| التوزيعات | `distribution.service.ts` | حساب وتنفيذ التوزيعات |
| المدفوعات | `payment.service.ts` | معالجة المدفوعات |
| الطلبات | `request.service.ts` | إدارة الطلبات |
| الموافقات | `approval.service.ts` | سير عمل الموافقات |
| التقارير | `report.service.ts` | إنشاء التقارير |
| الإشعارات | `notification.service.ts` | إرسال الإشعارات |
| السندات | `voucher.service.ts` | إدارة سندات الصرف |
| الميزانية | `budget.service.ts` | إدارة الميزانيات |
| البنوك | `bank-validation.service.ts` | التحقق من IBAN |

### `/src/pages/` - الصفحات

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Dashboard | `/dashboard` | لوحة التحكم الرئيسية |
| Beneficiaries | `/beneficiaries` | إدارة المستفيدين |
| Properties | `/properties` | إدارة العقارات |
| Funds | `/funds` | إدارة الصناديق |
| Accounting | `/accounting` | المحاسبة |
| Distributions | `/distributions` | التوزيعات |
| Reports | `/reports` | التقارير |
| Settings | `/settings` | الإعدادات |
| Login | `/login` | تسجيل الدخول |

---

## 🎨 نظام التصميم

### الملفات الأساسية
- `src/index.css` - المتغيرات CSS والتصميم
- `tailwind.config.ts` - إعدادات Tailwind

### ألوان التصميم (HSL)
```css
--primary: 142 76% 36%        /* الأخضر الرئيسي */
--secondary: 240 5% 96%       /* الرمادي الثانوي */
--accent: 142 76% 36%         /* لون التمييز */
--background: 0 0% 100%       /* الخلفية */
--foreground: 240 10% 4%      /* النص */
--muted: 240 5% 96%           /* العناصر الخافتة */
--destructive: 0 84% 60%      /* الأحمر للتحذير */
```

---

## 🔒 نظام الأدوار والصلاحيات

### الأدوار المتاحة
| الدور | الوصف | الصلاحيات |
|-------|-------|-----------|
| `admin` | مدير النظام | جميع الصلاحيات |
| `nazer` | الناظر | الموافقات النهائية، الإشراف |
| `accountant` | المحاسب | القيود المالية، التقارير |
| `cashier` | أمين الصندوق | الصرف والقبض |
| `staff` | موظف | العمليات اليومية |
| `beneficiary` | مستفيد | الوصول لبوابة المستفيد |

### حماية المسارات
```tsx
<ProtectedRoute allowedRoles={['admin', 'nazer']}>
  <Component />
</ProtectedRoute>
```

---

## 📦 التبعيات الرئيسية

| الحزمة | الإصدار | الاستخدام |
|--------|---------|-----------|
| React | ^18.3.1 | إطار العمل |
| TypeScript | - | Type Safety |
| Tailwind CSS | - | التصميم |
| Shadcn UI | - | مكونات UI |
| Supabase | ^2.81.1 | Backend |
| TanStack Query | ^5.90.9 | إدارة البيانات |
| React Router | ^6.30.1 | التوجيه |
| Recharts | ^3.4.1 | الرسوم البيانية |
| jsPDF | ^3.0.3 | تصدير PDF |
| Zod | ^3.25.76 | التحقق |

---

## 🧪 الاختبارات

### هيكل الاختبارات
```
src/__tests__/
├── e2e/           # اختبارات End-to-End
├── integration/   # اختبارات التكامل
├── unit/          # اختبارات الوحدات
└── fixtures/      # بيانات الاختبار

src/hooks/__tests__/ # اختبارات Hooks
```

### أوامر التشغيل
```bash
npm run test           # تشغيل الاختبارات
npm run test:coverage  # التغطية
npm run e2e            # E2E Tests
```

---

## 📝 معايير الكود

### التسمية
- **Components**: PascalCase (e.g., `BeneficiaryCard`)
- **Hooks**: camelCase مع `use` (e.g., `useBeneficiaries`)
- **Services**: camelCase مع `.service` (e.g., `beneficiary.service`)
- **Types**: PascalCase (e.g., `BeneficiaryData`)

### الملفات
- مكون واحد لكل ملف
- الحد الأقصى: 300 سطر
- استخدام barrel exports (`index.ts`)

---

**آخر تحديث**: 2025-11-27
