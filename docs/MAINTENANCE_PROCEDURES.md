# 🔧 دليل إجراءات الصيانة

## 📋 جدول الصيانة الشامل

### الصيانة اليومية (10 دقائق)

#### الصباح (9:00 صباحاً)
```bash
# 1. فحص صحة النظام
✓ افتح https://[your-app].lovableproject.com
✓ تحقق من تحميل الصفحة بشكل طبيعي
✓ سجل دخول كمستخدم Admin
✓ تحقق من عدم وجود أخطاء في Console

# 2. مراجعة التنبيهات
✓ افتح لوحة التحكم
✓ راجع التنبيهات النشطة
✓ حل التنبيهات البسيطة
✓ صعّد التنبيهات الحرجة

# 3. فحص الأداء
✓ راجع أوقات التحميل
✓ تحقق من استجابة API
✓ راجع استخدام قاعدة البيانات
```

#### المساء (5:00 مساءً)
```bash
# 1. مراجعة سجل اليوم
✓ راجع جميع الأنشطة
✓ تحقق من معدل الأخطاء
✓ وثق أي مشاكل ظهرت

# 2. النسخ الاحتياطي
✓ تحقق من إكمال النسخ التلقائي
✓ اختبر استرجاع عينة
```

---

### الصيانة الأسبوعية (ساعة واحدة)

#### كل يوم أحد (10:00 صباحاً)

##### 1. مراجعة قاعدة البيانات (15 دقيقة)
```sql
-- فحص حجم الجداول
SELECT 
  schemaname,
  tablename,
  pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS size
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC
LIMIT 10;

-- فحص الفهارس غير المستخدمة
SELECT 
  schemaname, 
  tablename, 
  indexname,
  pg_size_pretty(pg_relation_size(indexrelid::regclass)) AS index_size
FROM pg_stat_user_indexes
WHERE idx_scan = 0
  AND schemaname = 'public';
```

##### 2. تنظيف البيانات (15 دقيقة)
```sql
-- حذف التنبيهات القديمة المحلولة (أكثر من 30 يوم)
DELETE FROM system_alerts
WHERE status = 'resolved' 
  AND resolved_at < NOW() - INTERVAL '30 days';

-- تنظيف سجلات الأخطاء القديمة
DELETE FROM system_error_logs
WHERE created_at < NOW() - INTERVAL '90 days'
  AND severity IN ('info', 'low');

-- تنظيف سجلات العمليات القديمة
DELETE FROM audit_logs
WHERE created_at < NOW() - INTERVAL '180 days'
  AND severity = 'info';
```

##### 3. اختبار النسخ الاحتياطية (15 دقيقة)
```bash
# اختبار استرجاع النسخة الاحتياطية
1. اختر نسخة احتياطية عشوائية
2. استرجع جدول واحد فقط للاختبار
3. تحقق من سلامة البيانات
4. احذف البيانات المسترجعة
```

##### 4. تحديث التوثيق (15 دقيقة)
```markdown
- راجع التغييرات خلال الأسبوع
- حدث CHANGELOG.md
- حدث ملفات README إن لزم
- وثق أي مشاكل وحلولها
```

---

### الصيانة الشهرية (4 ساعات)

#### أول يوم من كل شهر

##### 1. تحليل الأداء الشامل (ساعة)
```typescript
// جمع المقاييس
const metrics = {
  // أداء قاعدة البيانات
  database: {
    avgQueryTime: "...",
    slowestQueries: [...],
    connectionPoolUsage: "..."
  },
  
  // أداء التطبيق
  application: {
    avgPageLoadTime: "...",
    apiResponseTime: "...",
    errorRate: "..."
  },
  
  // الاستخدام
  usage: {
    activeUsers: "...",
    totalRequests: "...",
    popularPages: [...]
  }
};

// إنشاء تقرير شهري
generateMonthlyReport(metrics);
```

##### 2. مراجعة الأمان (ساعة)
```sql
-- فحص صلاحيات المستخدمين
SELECT 
  p.full_name,
  r.role_name_ar,
  p.last_login_at,
  p.is_active
FROM profiles p
LEFT JOIN user_roles ur ON p.user_id = ur.user_id
LEFT JOIN roles r ON ur.role_id = r.id
WHERE p.is_active = true
ORDER BY p.last_login_at DESC;

-- فحص RLS policies
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;

-- فحص محاولات الدخول الفاشلة
SELECT 
  user_email,
  ip_address,
  COUNT(*) as attempts,
  MAX(created_at) as last_attempt
FROM login_attempts
WHERE success = false
  AND created_at > NOW() - INTERVAL '30 days'
GROUP BY user_email, ip_address
HAVING COUNT(*) > 5
ORDER BY attempts DESC;
```

##### 3. تحسين قاعدة البيانات (ساعة)
```sql
-- تحليل الجداول
ANALYZE;

-- فهرسة المطلوبة
CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_beneficiaries_status 
  ON beneficiaries(status) WHERE status = 'نشط';

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_payments_created_at 
  ON payment_vouchers(created_at DESC);

-- حذف الفهارس غير المستخدمة
-- (بعد التحقق من عدم استخدامها لمدة شهر)
```

##### 4. تقرير الصحة الشامل (ساعة)
```markdown
# تقرير الصحة الشهري - [الشهر] 2024

## الملخص التنفيذي
- الحالة العامة: [ممتاز/جيد/يحتاج تحسين]
- معدل الاستقرار: XX.X%
- متوسط وقت الاستجابة: X.Xs

## الإنجازات
- [قائمة الإنجازات]

## المشاكل المحلولة
- [قائمة المشاكل]

## التوصيات
- [التوصيات للشهر القادم]

## المقاييس التفصيلية
[الجداول والرسوم البيانية]
```

---

## 🚨 إجراءات الطوارئ

### سيناريو 1: موقع لا يعمل

```bash
# الخطوة 1: التحقق السريع (دقيقة واحدة)
1. افتح الموقع في متصفح incognito
2. تحقق من Console للأخطاء
3. افتح Network tab

# الخطوة 2: التشخيص (3 دقائق)
- هل الخطأ في Frontend أم Backend؟
- هل Supabase متاح؟
- هل هناك أخطاء في Database logs؟

# الخطوة 3: الإصلاح (حسب المشكلة)
Frontend:
  - Clear cache and reload
  - Check build status
  - Redeploy if needed

Backend:
  - Check Supabase status
  - Verify database connection
  - Check RLS policies

# الخطوة 4: التحقق
- اختبر الوظائف الرئيسية
- راقب لمدة 15 دقيقة
- وثق المشكلة والحل
```

### سيناريو 2: أداء بطيء

```bash
# التشخيص
1. افتح React Query DevTools
2. راجع Network tab
3. فحص Database slow queries

# الإصلاح الفوري
- أوقف الاستعلامات الثقيلة
- زد Cache time مؤقتاً
- فعّل query optimization

# الإصلاح الدائم
- أضف indexes مناسبة
- حسّن الاستعلامات البطيئة
- طبق lazy loading
```

### سيناريو 3: خرق أمني مشتبه به

```bash
# الإجراء الفوري (5 دقائق)
1. سجل خروج جميع المستخدمين
2. عطّل تسجيل الدخول مؤقتاً
3. راجع audit_logs فوراً

# التحقيق (30 دقيقة)
1. فحص سجلات تسجيل الدخول
2. راجع التغييرات الأخيرة
3. تحديد نطاق الخرق

# الإصلاح
1. أغلق الثغرة الأمنية
2. أعد تعيين كلمات المرور المتأثرة
3. أعد تفعيل النظام
4. راقب عن كثب لمدة 24 ساعة

# التقرير
- وثق المشكلة بالكامل
- أبلغ المستخدمين المتأثرين
- طبق تحسينات أمنية إضافية
```

---

## 📊 قوالب التقارير

### تقرير الصيانة اليومي
```markdown
# تقرير يومي - [التاريخ]

## الحالة العامة
✅ النظام يعمل بشكل طبيعي

## المقاييس
- وقت الاستجابة: X.Xs
- معدل الأخطاء: X.X%
- المستخدمون النشطون: XX

## الأنشطة
- [قائمة الأنشطة المهمة]

## المشاكل
- [المشاكل إن وجدت وكيف تم حلها]

## ملاحظات
- [أي ملاحظات إضافية]
```

### تقرير الصيانة الأسبوعي
```markdown
# تقرير أسبوعي - أسبوع [XX]

## ملخص الأسبوع
- إجمالي الطلبات: XXXXX
- متوسط الاستجابة: X.Xs
- نسبة الاستقرار: XX.X%

## التحسينات المطبقة
1. [قائمة التحسينات]

## المشاكل المحلولة
1. [قائمة المشاكل]

## خطة الأسبوع القادم
- [الخطط والتحسينات المقترحة]
```

---

## ✅ قائمة التحقق الشاملة

### قبل إطلاق تحديث
- [ ] اختبار شامل في بيئة التطوير
- [ ] مراجعة جميع التغييرات
- [ ] نسخة احتياطية كاملة
- [ ] إشعار المستخدمين (إن لزم)
- [ ] خطة rollback جاهزة
- [ ] فريق الدعم مستعد

### بعد إطلاق تحديث
- [ ] مراقبة فورية لمدة ساعة
- [ ] اختبار الوظائف الرئيسية
- [ ] مراجعة error logs
- [ ] تأكيد عدم وجود regression
- [ ] توثيق التغييرات
- [ ] متابعة لمدة 24 ساعة

---

**آخر تحديث:** 26 نوفمبر 2024
**المسؤول:** فريق التطوير
**المراجعة التالية:** 1 ديسمبر 2024
