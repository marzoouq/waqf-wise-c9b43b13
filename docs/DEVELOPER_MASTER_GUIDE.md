# 📘 دليل المطور الشامل | Developer Master Guide

**النسخة:** 2.6.6  
**التاريخ:** ديسمبر 2025  
**الحالة:** 95% مكتمل - جاهز للإنتاج

---

## 📝 سجل التغييرات

| التاريخ | الإصدار | التغيير |
|---------|---------|---------|
| 2025-12-03 | 2.6.6 | تحسين سرعة الصفحة الترحيبية (< 0.5s) |
| 2025-12-03 | 2.6.5 | تحسين LCP للصفحة الترحيبية |
| 2025-12-02 | 2.6.4 | تحسين الأداء 70% + تنظيم 152 hook |
| 2025-12-02 | 2.6.0 | إصلاحات أمنية + RLS |

---

## 🎯 نظرة عامة

منصة إدارة الوقف الإلكترونية هي تطبيق ويب متكامل لإدارة الأوقاف الإسلامية.

### التقنيات الأساسية
- **Frontend:** React 18.3 + TypeScript + Vite + Tailwind CSS
- **Backend:** Supabase (PostgreSQL + Edge Functions)
- **State Management:** TanStack React Query v5
- **Forms:** React Hook Form + Zod
- **UI Components:** Radix UI + shadcn/ui

---

## 📊 حالة المشروع

### الإحصائيات الرئيسية

```
📊 الكود:
├─ 74 صفحة
├─ 40+ مجلد مكونات
├─ 152 Custom Hook (18 مجلد فرعي)
├─ 39 Edge Function
├─ 14 خدمة (Service)
└─ ~55,000 سطر TypeScript/React

🗄️ قاعدة البيانات:
├─ 162 جدول
├─ 10 Views
├─ 138 دالة
├─ 100% RLS Coverage
└─ 15 مستفيد، 9 توزيعات، 3 عقارات

🔒 الأمان:
├─ 7 أدوار (Roles)
├─ 50+ صلاحية (Permissions)
├─ 50 إعداد شفافية
└─ Audit Logs شاملة
```

---

## 🏗️ البنية المعمارية

### هيكل المجلدات

```
src/
├─ pages/              # 74 صفحة
├─ components/         # 40+ مجلد
├─ hooks/              # 152 hook (18 مجلد فرعي)
│  ├─ accounting/      # hooks المحاسبة
│  ├─ approval/        # hooks الموافقات
│  ├─ archive/         # hooks الأرشيف
│  ├─ auth/            # hooks المصادقة
│  ├─ beneficiary/     # hooks المستفيدين
│  ├─ contracts/       # hooks العقود
│  ├─ dashboard/       # hooks لوحات التحكم
│  ├─ distributions/   # hooks التوزيعات
│  ├─ export/          # hooks التصدير
│  ├─ fiscal/          # hooks السنة المالية
│  ├─ loans/           # hooks القروض
│  ├─ monitoring/      # hooks المراقبة
│  ├─ notifications/   # hooks الإشعارات
│  ├─ properties/      # hooks العقارات
│  ├─ reports/         # hooks التقارير
│  ├─ security/        # hooks الأمان
│  ├─ settings/        # hooks الإعدادات
│  └─ ui/              # hooks واجهة المستخدم
├─ services/           # 14 خدمة
├─ lib/                # مكتبات مساعدة
└─ integrations/       # تكاملات خارجية
```

---

## 🔐 نظام الأمان

### الأدوار (7 Roles)

```typescript
type AppRole = 
  | "nazer"       // الناظر - صلاحيات كاملة
  | "admin"       // مدير النظام
  | "accountant"  // محاسب
  | "cashier"     // أمين الصندوق
  | "archivist"   // أرشيفي
  | "beneficiary" // مستفيد
  | "user";       // مستخدم عادي
```

### Row Level Security (RLS)
- ✅ 100% Coverage لجميع الـ162 جدول
- ✅ سياسات مُبسطة ومحسنة (v2.6.0)
- ✅ حماية البيانات الحساسة

---

## 🔧 Custom Hooks (152)

### Core Hooks
```typescript
useAuth()           // المصادقة
useUserRole()       // الدور الحالي
usePermissions()    // الصلاحيات
useBeneficiaries()  // المستفيدين
useProperties()     // العقارات
useDistributions()  // التوزيعات
```

### Feature Hooks
```typescript
useDistributionEngine()     // محرك التوزيع
useApprovalWorkflows()      // مسارات الموافقات
useBankReconciliation()     // التسوية البنكية
useAIInsights()             // الرؤى الذكية
```

---

## 🔥 Edge Functions (39)

### الأساسية
- `backup-database` - النسخ الاحتياطي (موحّد)
- `daily-notifications` - الإشعارات (موحّد)
- `chatbot` - المساعد الذكي
- `ocr-document` - استخراج النصوص

### المالية
- `auto-create-journal` - القيود التلقائية
- `simulate-distribution` - محاكاة التوزيع
- `zatca-submit` - الفوترة الإلكترونية

---

## 📈 تحسينات الأداء

### v2.6.6 - تحميل فوري للصفحة الترحيبية
```
المشكلة: الصفحة الترحيبية تظهر "جاري التحميل..."
الحل: 
- نقل التهيئة الثقيلة (ErrorTracker, SelfHealing) إلى MainLayout
- فصل Suspense للصفحات العامة
- تحميل فوري (eager) للصفحة الترحيبية
- إضافة PUBLIC_ROUTES في AuthContext
النتيجة: تحميل فوري < 0.5 ثانية
```

### v2.6.5 - تحسين LCP
```
المشكلة: LCP ~10 ثواني للصفحة الترحيبية
الحل: نقل مكونات المصادقة من App.tsx إلى MainLayout.tsx
النتيجة: LCP < 2.5 ثانية
```

### v2.6.4 - تحسين Dashboard
```
المشكلة: Dashboard يستغرق ~3.3 ثانية
الحل: Promise.all للاستعلامات المتوازية
النتيجة: ~1.1 ثانية (-66%)
```

---

## 📝 أفضل الممارسات

### 1. استخدام الخصائص الصحيحة
```typescript
// ❌ خطأ
property.property_type

// ✅ صحيح
property.type
```

### 2. الاستعلامات المتوازية
```typescript
// ✅ صحيح
const [a, b] = await Promise.all([
  supabase.from('table1').select(),
  supabase.from('table2').select(),
]);
```

### 3. تحميل المكونات
```typescript
// ❌ خطأ: مكونات المصادقة في App.tsx
// ✅ صحيح: مكونات المصادقة في MainLayout.tsx
```

---

## 🔗 روابط مفيدة

| الملف | الوصف |
|-------|-------|
| [LATEST_FIXES.md](./LATEST_FIXES.md) | آخر الإصلاحات |
| [PERFORMANCE.md](./PERFORMANCE.md) | تقرير الأداء |
| [CLEANUP.md](./CLEANUP.md) | سجل التنظيف |
| [SECURITY_FIX_PLAN.md](./SECURITY_FIX_PLAN.md) | خطة الأمان |

---

**آخر تحديث:** 3 ديسمبر 2025
