# الكود المُهمل وبدائله

> آخر تحديث: 2026-01-18

## ملخص

| العنصر | النوع | البديل | تاريخ الإهمال |
|--------|-------|--------|---------------|
| `useKPIs` | Hook | `useUnifiedKPIs` | 2026-01-17 |
| `QUERY_CONFIG` (قديم) | Constant | `@/infrastructure/react-query` | 2026-01-17 |

---

## 1. useKPIs → useUnifiedKPIs

### التفاصيل

| العنصر | القيمة |
|--------|--------|
| **الملف القديم** | `src/hooks/dashboard/useKPIs.ts` |
| **البديل** | `src/hooks/dashboard/useUnifiedKPIs.ts` |
| **السبب** | توحيد مصادر KPI لضمان تناسق الأرقام |
| **تاريخ الإهمال** | 2026-01-17 |

### الاستخدام القديم ❌

```typescript
import { useKPIs } from '@/hooks/dashboard/useKPIs';

const { kpis, isLoading } = useKPIs('financial');
```

### الاستخدام الجديد ✅

```typescript
import { useUnifiedKPIs } from '@/hooks/dashboard/useUnifiedKPIs';

const { data, isLoading, error } = useUnifiedKPIs();
```

### الفروقات

| الميزة | useKPIs | useUnifiedKPIs |
|--------|---------|----------------|
| مصدر البيانات | متعدد | موحد (KPIService) |
| الكاش | منفصل | مشترك |
| الأداء | متوسط | محسّن |
| التناسق | ⚠️ قد يختلف | ✅ متسق 100% |

### خطة الإزالة

1. ✅ تم إنشاء `useUnifiedKPIs` كبديل
2. ✅ تم تحديث جميع اللوحات لاستخدام البديل
3. ⏳ `useKPIs` محتفظ به للتوافق العكسي
4. 📅 الإزالة المخططة: Q2 2026

---

## 2. QUERY_CONFIG (إعادة التصدير)

### التفاصيل

| العنصر | القيمة |
|--------|--------|
| **الموقع القديم** | `src/lib/query-keys/index.ts` |
| **المصدر الجديد** | `@/infrastructure/react-query` |
| **السبب** | تنظيم الكود وفصل البنية التحتية |
| **تاريخ الإهمال** | 2026-01-17 |

### الاستخدام القديم ❌

```typescript
import { QUERY_CONFIG } from '@/lib/query-keys';

const { staleTime } = QUERY_CONFIG.default;
```

### الاستخدام الجديد ✅

```typescript
import { QUERY_CONFIG } from '@/infrastructure/react-query';

const { staleTime } = QUERY_CONFIG.default;
```

### ملاحظة

الاستيراد القديم يعمل حالياً لأنه يُعيد التصدير من المصدر الجديد:

```typescript
// src/lib/query-keys/index.ts
export { QUERY_CONFIG } from '@/infrastructure/react-query';
```

لكن يُنصح بالاستيراد مباشرة من المصدر الجديد.

---

## 3. قواعد التعامل مع الكود المُهمل

### للمطورين

1. **لا تستخدم** الكود المُهمل في الميزات الجديدة
2. **حدّث** الكود الموجود عند التعديل عليه
3. **أبلغ** عن أي استخدام جديد للكود المُهمل

### للمراجعين

1. **ارفض** أي PR يستخدم كود مُهمل
2. **اقترح** البديل المناسب
3. **تأكد** من عدم إضافة تبعيات جديدة على الكود المُهمل

---

## 4. الجدول الزمني للإزالة

| العنصر | تاريخ الإهمال | تاريخ الإزالة المخطط |
|--------|---------------|---------------------|
| `useKPIs` | 2026-01-17 | Q2 2026 |
| `QUERY_CONFIG` (قديم) | 2026-01-17 | Q3 2026 |

---

## 5. التحديثات

| التاريخ | التغيير |
|---------|---------|
| 2026-01-18 | إنشاء الملف |
| 2026-01-17 | إهمال `useKPIs` |
| 2026-01-17 | نقل `QUERY_CONFIG` إلى البنية التحتية |
