# 📁 هيكل منصة إدارة الوقف الإلكترونية

## 📊 إحصائيات التطبيق

| الفئة | العدد |
|-------|-------|
| **إجمالي الصفحات** | 69 صفحة |
| **المكونات (Components)** | 41 مجلد |
| **الـ Hooks المخصصة** | 148 hook |
| **مكونات UI (Shadcn)** | 51 مكون |
| **Edge Functions** | 41 وظيفة خادم |
| **السياقات (Contexts)** | 2 سياق |
| **الأدوات المساعدة (Lib)** | 45+ ملف |

---

## 🏗️ الهيكل العام للتطبيق

```
src/
├── main.tsx                 # نقطة الدخول - التحقق من الإصدار وتحميل التطبيق
├── App.tsx                  # التوجيه الرئيسي وإعداد Providers
├── index.css                # نظام التصميم (Design System)
│
├── pages/                   # 69 صفحة
├── components/              # 41 مجلد مكونات
├── hooks/                   # 148 hook مخصص
├── contexts/                # 2 سياق (Auth, Settings)
├── lib/                     # أدوات ومساعدات
├── integrations/            # تكامل Supabase
├── services/                # خدمات API
├── types/                   # تعريفات TypeScript
├── utils/                   # أدوات عامة
└── config/                  # إعدادات التطبيق
```

---

## 👥 أدوار المستخدمين ولوحات التحكم

### 1. 👑 الناظر (Nazer)
**المسار**: `/nazer-dashboard`
**الصلاحيات**: كاملة - صاحب القرار النهائي

```
┌─────────────────────────────────────────────────────────┐
│                   لوحة تحكم الناظر                      │
├─────────────────────────────────────────────────────────┤
│  📊 إحصائيات سريعة                                      │
│  ├── إجمالي عوائد الوقف                                │
│  ├── إجمالي المدفوعات                                  │
│  ├── الرصيد المتاح                                     │
│  └── الطلبات المعلقة                                   │
├─────────────────────────────────────────────────────────┤
│  ⚡ إجراءات سريعة                                       │
│  ├── الموافقة على التوزيع                              │
│  ├── إنشاء توزيع جديد                                  │
│  ├── استعراض طلبات المستفيدين                          │
│  └── القرارات الحوكمية                                 │
├─────────────────────────────────────────────────────────┤
│  📈 الرسوم البيانية                                    │
│  ├── توزيع الحسابات                                    │
│  ├── مقارنة الميزانيات                                 │
│  └── اتجاهات العوائد                                   │
└─────────────────────────────────────────────────────────┘
```

**الصفحات المتاحة للناظر**:
- `/nazer-dashboard` - لوحة التحكم الرئيسية
- `/beneficiaries` - إدارة المستفيدين
- `/properties` - إدارة العقارات
- `/accounting` - المحاسبة
- `/budgets` - الميزانيات
- `/approvals` - الموافقات
- `/reports` - التقارير
- `/governance-decisions` - القرارات الحوكمية
- `/users` - إدارة المستخدمين
- `/settings` - الإعدادات

---

### 2. 🛡️ مدير النظام (Admin)
**المسار**: `/admin-dashboard`
**الصلاحيات**: إدارة النظام والمستخدمين

```
┌─────────────────────────────────────────────────────────┐
│                   لوحة تحكم المدير                      │
├─────────────────────────────────────────────────────────┤
│  🔧 إدارة النظام                                        │
│  ├── إدارة المستخدمين والأدوار                         │
│  ├── سجلات التدقيق                                     │
│  ├── مراقبة الأداء                                     │
│  └── إعدادات الأمان                                    │
├─────────────────────────────────────────────────────────┤
│  📊 مراقبة النظام                                      │
│  ├── أخطاء النظام                                      │
│  ├── صحة الخدمات                                       │
│  └── التكاملات                                         │
├─────────────────────────────────────────────────────────┤
│  🛠️ أدوات المطور                                       │
│  ├── دليل المطور                                       │
│  ├── اختبارات النظام                                   │
│  └── بيانات الاختبار                                   │
└─────────────────────────────────────────────────────────┘
```

**الصفحات الحصرية للمدير**:
- `/admin-dashboard` - لوحة التحكم
- `/users` - إدارة المستخدمين
- `/settings/roles` - إدارة الأدوار
- `/settings/permissions` - إدارة الصلاحيات
- `/security` - لوحة الأمان
- `/performance` - لوحة الأداء
- `/audit-logs` - سجلات التدقيق
- `/system-monitoring` - مراقبة النظام
- `/system-maintenance` - صيانة النظام
- `/developer-guide` - دليل المطور
- `/developer-tools` - أدوات المطور

---

### 3. 💰 المحاسب (Accountant)
**المسار**: `/accountant-dashboard`
**الصلاحيات**: العمليات المالية والتقارير

```
┌─────────────────────────────────────────────────────────┐
│                   لوحة تحكم المحاسب                     │
├─────────────────────────────────────────────────────────┤
│  💵 الإحصائيات المالية                                  │
│  ├── إجمالي الإيرادات                                  │
│  ├── إجمالي المصروفات                                  │
│  ├── صافي الدخل                                        │
│  └── قيود معلقة                                        │
├─────────────────────────────────────────────────────────┤
│  📋 المهام اليومية                                      │
│  ├── القيود اليومية                                    │
│  ├── التسوية البنكية                                   │
│  ├── الفواتير                                          │
│  └── سندات الصرف                                       │
├─────────────────────────────────────────────────────────┤
│  📊 التقارير المالية                                    │
│  ├── ميزان المراجعة                                    │
│  ├── قائمة الدخل                                       │
│  └── قائمة المركز المالي                               │
└─────────────────────────────────────────────────────────┘
```

**الصفحات المتاحة للمحاسب**:
- `/accountant-dashboard` - لوحة التحكم
- `/accounting` - المحاسبة
- `/budgets` - الميزانيات
- `/invoices` - الفواتير
- `/payment-vouchers` - سندات الصرف
- `/approvals` - الموافقات
- `/reports` - التقارير
- `/beneficiaries` - المستفيدين (عرض)
- `/properties` - العقارات (عرض)

---

### 4. 💳 أمين الصندوق (Cashier)
**المسار**: `/cashier-dashboard`
**الصلاحيات**: تنفيذ المدفوعات

```
┌─────────────────────────────────────────────────────────┐
│                 لوحة تحكم أمين الصندوق                  │
├─────────────────────────────────────────────────────────┤
│  💰 إحصائيات الصرف                                      │
│  ├── مدفوعات اليوم                                     │
│  ├── مدفوعات الشهر                                     │
│  ├── سندات معلقة                                       │
│  └── تحويلات بنكية                                     │
├─────────────────────────────────────────────────────────┤
│  📝 المهام                                              │
│  ├── سندات الصرف المعتمدة                              │
│  ├── تحضير التحويلات البنكية                           │
│  └── تأكيد الصرف                                       │
└─────────────────────────────────────────────────────────┘
```

**الصفحات المتاحة لأمين الصندوق**:
- `/cashier-dashboard` - لوحة التحكم
- `/payment-vouchers` - سندات الصرف
- `/payments` - المدفوعات
- `/bank-transfers` - التحويلات البنكية

---

### 5. 📁 الأرشيفي (Archivist)
**المسار**: `/archivist-dashboard`
**الصلاحيات**: إدارة المستندات والأرشيف

```
┌─────────────────────────────────────────────────────────┐
│                   لوحة تحكم الأرشيفي                    │
├─────────────────────────────────────────────────────────┤
│  📊 إحصائيات الأرشيف                                    │
│  ├── إجمالي المستندات                                  │
│  ├── مستندات جديدة                                     │
│  ├── مستندات بحاجة تصنيف                               │
│  └── المجلدات                                          │
├─────────────────────────────────────────────────────────┤
│  🗂️ المهام                                              │
│  ├── تصنيف المستندات                                   │
│  ├── أرشفة العقود                                      │
│  └── إدارة المرفقات                                    │
└─────────────────────────────────────────────────────────┘
```

**الصفحات المتاحة للأرشيفي**:
- `/archivist-dashboard` - لوحة التحكم
- `/archive` - الأرشيف

---

### 6. 👤 المستفيد (Beneficiary)
**المسار**: `/beneficiary-dashboard`
**الصلاحيات**: وصول محدود للملف الشخصي

```
┌─────────────────────────────────────────────────────────┐
│                   لوحة تحكم المستفيد                    │
├─────────────────────────────────────────────────────────┤
│  💵 معلوماتي المالية                                    │
│  ├── رصيدي الحالي                                      │
│  ├── آخر دفعة                                          │
│  ├── إجمالي المستلم                                    │
│  └── طلبات معلقة                                       │
├─────────────────────────────────────────────────────────┤
│  📋 خدماتي                                              │
│  ├── تقديم طلب جديد                                    │
│  ├── متابعة طلباتي                                     │
│  ├── كشف حسابي                                         │
│  └── مستنداتي                                          │
├─────────────────────────────────────────────────────────┤
│  📞 الدعم                                               │
│  ├── الرسائل                                           │
│  └── الدعم الفني                                       │
└─────────────────────────────────────────────────────────┘
```

**الصفحات المتاحة للمستفيد**:
- `/beneficiary-dashboard` - لوحة التحكم (مستقلة)
- `/beneficiary-portal` - البوابة
- `/beneficiary/requests` - طلباتي
- `/beneficiary/account-statement` - كشف الحساب
- `/beneficiary/reports` - تقاريري
- `/beneficiary-support` - الدعم
- `/messages` - الرسائل

---

## 📄 قائمة الصفحات الكاملة (69 صفحة)

### صفحات عامة (Public)
| الصفحة | المسار | الوصف |
|--------|--------|-------|
| Login | `/login` | تسجيل الدخول |
| Signup | `/signup` | إنشاء حساب |
| Install | `/install` | تثبيت PWA |
| Unauthorized | `/unauthorized` | غير مصرح |
| NotFound | `*` | صفحة 404 |

### لوحات التحكم (6 لوحات)
| الصفحة | المسار | الدور |
|--------|--------|-------|
| Dashboard | `/dashboard` | عام |
| NazerDashboard | `/nazer-dashboard` | الناظر |
| AdminDashboard | `/admin-dashboard` | المدير |
| AccountantDashboard | `/accountant-dashboard` | المحاسب |
| CashierDashboard | `/cashier-dashboard` | أمين الصندوق |
| ArchivistDashboard | `/archivist-dashboard` | الأرشيفي |
| BeneficiaryDashboard | `/beneficiary-dashboard` | المستفيد |

### إدارة المستفيدين
| الصفحة | المسار |
|--------|--------|
| Beneficiaries | `/beneficiaries` |
| BeneficiaryProfile | `/beneficiaries/:id` |
| Families | `/families` |
| FamilyDetails | `/families/:id` |

### المالية والمحاسبة
| الصفحة | المسار |
|--------|--------|
| Accounting | `/accounting` |
| Budgets | `/budgets` |
| Invoices | `/invoices` |
| PaymentVouchers | `/payment-vouchers` |
| Payments | `/payments` |
| Loans | `/loans` |
| AllTransactions | `/all-transactions` |
| BankTransfers | `/bank-transfers` |

### العقارات والأصول
| الصفحة | المسار |
|--------|--------|
| Properties | `/properties` |
| Funds | `/funds` |
| WaqfUnits | `/waqf-units` |

### الطلبات والموافقات
| الصفحة | المسار |
|--------|--------|
| Requests | `/requests` |
| Approvals | `/approvals` |
| StaffRequestsManagement | `/staff/requests` |
| EmergencyAidManagement | `/emergency-aid` |

### الأرشيف والمستندات
| الصفحة | المسار |
|--------|--------|
| Archive | `/archive` |

### التقارير
| الصفحة | المسار |
|--------|--------|
| Reports | `/reports` |
| CustomReports | `/custom-reports` |
| AIInsights | `/ai-insights` |

### الإعدادات والنظام
| الصفحة | المسار |
|--------|--------|
| Settings | `/settings` |
| AdvancedSettings | `/advanced-settings` |
| TransparencySettings | `/transparency-settings` |
| NotificationSettings | `/notification-settings` |
| Users | `/users` |
| RolesManagement | `/settings/roles` |
| PermissionsManagement | `/settings/permissions` |

### الأمان والمراقبة
| الصفحة | المسار |
|--------|--------|
| SecurityDashboard | `/security` |
| PerformanceDashboard | `/performance` |
| AuditLogs | `/audit-logs` |
| SystemMonitoring | `/system-monitoring` |
| SystemErrorLogs | `/system-error-logs` |
| SystemMaintenance | `/system-maintenance` |

### التواصل والدعم
| الصفحة | المسار |
|--------|--------|
| Notifications | `/notifications` |
| Messages | `/messages` |
| Support | `/support` |
| SupportManagement | `/support-management` |
| Chatbot | `/chatbot` |
| KnowledgeBase | `/knowledge-base` |

### الحوكمة
| الصفحة | المسار |
|--------|--------|
| GovernanceDecisions | `/governance-decisions` |
| DecisionDetails | `/governance-decisions/:id` |
| IntegrationsManagement | `/integrations` |

### أدوات المطور
| الصفحة | المسار |
|--------|--------|
| DeveloperGuide | `/developer-guide` |
| DeveloperTools | `/developer-tools` |
| ProjectDocumentation | `/project-documentation` |
| DesignPreview | `/design-preview` |
| SystemTesting | `/system-testing` |
| TestDataManager | `/test-data-manager` |
| ComprehensiveTestingDashboard | `/comprehensive-testing` |

---

## 🔧 المكونات الرئيسية (Components)

### مجلدات المكونات (41 مجلد)
```
components/
├── accounting/          # المحاسبة (قيود، تقارير، تسوية)
├── approvals/           # نظام الموافقات
├── archive/             # الأرشفة والمستندات
├── auth/                # المصادقة والحماية
├── beneficiaries/       # إدارة المستفيدين
├── beneficiary/         # مكونات بوابة المستفيد
├── budgets/             # الميزانيات
├── chatbot/             # روبوت المحادثة
├── contracts/           # العقود
├── dashboard/           # لوحات التحكم
├── design-preview/      # معاينة التصميم
├── developer/           # أدوات المطور
├── distributions/       # التوزيعات
├── documentation/       # التوثيق
├── error/               # معالجة الأخطاء
├── families/            # العائلات
├── funds/               # الصناديق
├── governance/          # الحوكمة
├── invoices/            # الفواتير
├── layout/              # التخطيط العام
├── loans/               # القروض
├── maintenance/         # الصيانة
├── messages/            # الرسائل
├── mobile/              # الجوال
├── notifications/       # الإشعارات
├── payments/            # المدفوعات
├── performance/         # الأداء
├── permissions/         # الصلاحيات
├── properties/          # العقارات
├── rental/              # الإيجارات
├── reports/             # التقارير
├── requests/            # الطلبات
├── settings/            # الإعدادات
├── shared/              # مشتركة
├── support/             # الدعم
├── system/              # النظام
├── ui/                  # مكونات Shadcn (51)
├── unified/             # مكونات موحدة
├── users/               # المستخدمين
├── waqf/                # الوقف
└── zatca/               # الفوترة الإلكترونية
```

---

## 🪝 الـ Hooks المخصصة (148 hook)

### حسب الفئة:

#### المصادقة والأمان
- `useAuth` - إدارة المصادقة
- `useBiometricAuth` - المصادقة البيومترية
- `useActiveSessions` - الجلسات النشطة
- `useIdleTimeout` - مهلة الخمول
- `useEncryption` - التشفير
- `useLeakedPassword` - فحص كلمات المرور المسربة

#### البيانات والاستعلامات
- `useBeneficiaries` - المستفيدين
- `useProperties` - العقارات
- `useFunds` - الصناديق
- `useAccounts` - الحسابات
- `useJournalEntries` - القيود اليومية
- `useDistributions` - التوزيعات
- `useContracts` - العقود
- `useLoans` - القروض

#### واجهة المستخدم
- `use-mobile` - اكتشاف الجوال
- `use-toast` - الإشعارات
- `useKeyboardShortcuts` - اختصارات لوحة المفاتيح
- `useFocusManagement` - إدارة التركيز

#### الأداء
- `usePerformanceMonitor` - مراقبة الأداء
- `useVirtualization` - التمثيل الافتراضي
- `useImageOptimization` - تحسين الصور
- `useDebouncedCallback` - تأخير الاستدعاء

---

## ⚡ Edge Functions (41 وظيفة)

### وظائف المصادقة
- `biometric-auth` - المصادقة البيومترية
- `check-leaked-password` - فحص كلمات المرور
- `create-beneficiary-accounts` - إنشاء حسابات المستفيدين
- `admin-manage-beneficiary-password` - إدارة كلمات مرور المستفيدين
- `reset-user-password` - إعادة تعيين كلمة المرور

### وظائف الذكاء الاصطناعي
- `chatbot` - روبوت المحادثة
- `generate-ai-insights` - توليد رؤى AI
- `generate-smart-alerts` - تنبيهات ذكية
- `intelligent-search` - البحث الذكي
- `auto-classify-document` - تصنيف تلقائي
- `extract-invoice-data` - استخراج بيانات الفواتير
- `ocr-document` - التعرف على النص
- `property-ai-assistant` - مساعد العقارات

### وظائف المالية
- `auto-create-journal` - إنشاء القيود تلقائياً
- `simulate-distribution` - محاكاة التوزيع
- `generate-distribution-summary` - ملخص التوزيع
- `zatca-submit` - إرسال فاتورة ZATCA

### وظائف النظام
- `backup-database` - نسخ احتياطي
- `restore-database` - استعادة
- `daily-backup` - نسخ يومي
- `enhanced-backup` - نسخ محسن
- `cleanup-old-files` - تنظيف الملفات
- `cleanup-sensitive-files` - تنظيف الملفات الحساسة
- `scheduled-cleanup` - تنظيف مجدول
- `log-error` - تسجيل الأخطاء
- `execute-auto-fix` - إصلاح تلقائي

### وظائف الإشعارات
- `send-notification` - إرسال إشعار
- `send-push-notification` - إشعار Push
- `notify-admins` - إشعار المديرين
- `notify-disclosure-published` - إشعار نشر الإفصاح
- `daily-notifications` - إشعارات يومية
- `contract-renewal-alerts` - تنبيهات تجديد العقود

### وظائف الأمان
- `encrypt-file` - تشفير ملف
- `decrypt-file` - فك تشفير
- `secure-delete-file` - حذف آمن

### وظائف التقارير
- `generate-scheduled-report` - تقرير مجدول
- `send-invoice-email` - إرسال فاتورة بالبريد

---

## 🔄 تدفق عمل التطبيق

```
┌──────────────────────────────────────────────────────────────┐
│                         main.tsx                              │
│  1. تهيئة أدوات التصحيح                                       │
│  2. التحقق من الإصدار (checkAndForceUpdate)                   │
│  3. تحميل التطبيق إذا لم يكن هناك تحديث                       │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                          App.tsx                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ GlobalErrorBoundary                                      │ │
│  │  └─ QueryClientProvider                                  │ │
│  │      └─ ThemeProvider                                    │ │
│  │          └─ AuthProvider                                 │ │
│  │              └─ SettingsProvider                         │ │
│  │                  └─ TooltipProvider                      │ │
│  │                      └─ BrowserRouter                    │ │
│  │                          └─ Routes                       │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────┬────────────────────────────────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
┌───────────────────┐       ┌───────────────────────────┐
│   Public Routes   │       │    Protected Routes       │
│  - /login         │       │  ┌─────────────────────┐  │
│  - /signup        │       │  │   ProtectedRoute    │  │
│  - /install       │       │  │    + MainLayout     │  │
│  - /unauthorized  │       │  │      + Page         │  │
└───────────────────┘       │  └─────────────────────┘  │
                            └───────────────────────────┘
```

---

## 🎨 نظام التصميم (Design System)

### الألوان (من index.css)
```css
--background: hsl(...)     /* خلفية */
--foreground: hsl(...)     /* نص */
--primary: hsl(...)        /* اللون الأساسي */
--secondary: hsl(...)      /* اللون الثانوي */
--accent: hsl(...)         /* لون التمييز */
--muted: hsl(...)          /* لون خافت */
--destructive: hsl(...)    /* لون التحذير */
```

### المكونات (Shadcn UI)
51 مكون جاهز للاستخدام:
- Button, Input, Select, Checkbox, Radio
- Card, Dialog, Sheet, Drawer
- Table, Tabs, Accordion
- Toast, Tooltip, Popover
- Calendar, DatePicker
- وغيرها...

---

## 📅 آخر تحديث
**التاريخ**: 2025-11-28
