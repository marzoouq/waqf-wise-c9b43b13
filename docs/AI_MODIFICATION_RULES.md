# قواعد التعديل الجنائية للذكاء الاصطناعي
# AI Forensic Modification Rules

> **آخر تحديث:** 2026-01-19
> **الإصدار:** 1.0.0

---

## المبدأ الأساسي

أي ملف تم فحصه جنائياً والتأكد من:
- ✅ نظافته من الأخطاء
- ✅ ارتباطه 100% بالنظام
- ✅ عدم وجود ملاحظات أو مشاكل

→ **يُضاف تلقائياً للملفات المحمية ويُمنع التعديل العشوائي**

---

## مستويات الحماية

### 🔴 CRITICAL (لا تعديل أبداً إلا بموافقة كتابية صريحة)

| الملف | الوصف |
|-------|-------|
| `src/contexts/AuthContext.tsx` | سياق المصادقة الرئيسي |
| `src/components/auth/ProtectedRoute.tsx` | حماية المسارات |
| `src/components/dashboard/admin/AdminDashboard.tsx` | لوحة المشرف |
| `src/components/dashboard/nazer/NazerDashboard.tsx` | لوحة الناظر |
| `src/lib/constants.ts` | الثوابت الموحدة |
| `src/lib/query-keys.ts` | مفاتيح الاستعلام |
| `src/services/dashboard/kpi.service.ts` | خدمة KPI الموحدة |

### 🟠 HIGH (تتطلب موافقة صريحة قبل التعديل)

| الفئة | الملفات |
|-------|---------|
| صفحة المستأجرين | `Tenants.tsx`, `TenantDetails.tsx`, `tenant.service.ts`, `useTenants.ts` |
| صفحة المستفيدين | `Beneficiaries.tsx`, `BeneficiaryProfile.tsx`, `beneficiary.service.ts` |
| خدمات مصادر الحقيقة | `kpi.service.ts`, `property-stats.service.ts`, `maintenance.service.ts` |
| Hooks الموحدة | `useUnifiedKPIs.ts`, `useCollectionStats.ts`, `useTenants.ts` |
| سياسات RLS المفحوصة | جميع السياسات على الجداول الحساسة |

### 🟡 MEDIUM (تنبيه قبل التعديل)

| الفئة | الملفات |
|-------|---------|
| مكونات UI المفحوصة | جميع المكونات في `src/components/` المفحوصة |
| صفحات التقارير | `Reports.tsx`, `SecurityDashboard.tsx`, `SystemMonitoring.tsx` |
| Edge Functions المختبرة | جميع الوظائف في `supabase/functions/` المختبرة |

---

## آلية التأمين التلقائي

عند إتمام فحص أي ملف:

```
1. ✅ تأكيد نظافة الملف من الأخطاء
2. ✅ تأكيد الارتباط الصحيح بالنظام
3. ✅ تأكيد عدم وجود ملاحظات
4. 🔒 إضافة الملف لقائمة الحماية تلقائياً
5. 📝 توثيق تاريخ ونتيجة الفحص في AUDIT_LOG.md
6. 📝 تحديث PROTECTED_FILES.md
```

---

## إجراءات قبل أي تعديل على ملف محمي

### الخطوة 1: التنبيه
```
⚠️ تنبيه: الملف [اسم الملف] محمي جنائياً
- مستوى الحماية: [CRITICAL/HIGH/MEDIUM]
- تاريخ الفحص: [التاريخ]
- نتيجة الفحص: نظيف 100%
```

### الخطوة 2: عرض التغيير المقترح
```
📝 التغيير المقترح:
- السبب: [سبب التعديل]
- الملف: [مسار الملف]
- الكود الحالي: [الكود]
- الكود الجديد: [الكود]
```

### الخطوة 3: انتظار الموافقة
```
🔐 للمتابعة، يرجى كتابة:
"أوافق على التعديل"
```

### الخطوة 4: التوثيق
بعد الموافقة، يتم:
- تسجيل التعديل في `docs/AUDIT_LOG.md`
- تحديث تاريخ آخر تعديل
- ذكر سبب التعديل

### الخطوة 5: التنفيذ
- تنفيذ التعديل فقط بعد إتمام جميع الخطوات السابقة

---

## الحالات المستثناة

### يُسمح بالتعديل بدون موافقة في:
1. **إصلاح أخطاء بناء حرجة** تمنع التشغيل
2. **تحديث التبعيات الأمنية** العاجلة
3. **إصلاح ثغرات أمنية** مكتشفة

### في هذه الحالات:
- يتم التنفيذ الفوري
- يتم التوثيق في AUDIT_LOG.md
- يتم إبلاغ المستخدم بالتغييرات

---

## العقوبات عند مخالفة القواعد

إذا تم تعديل ملف محمي بدون موافقة:
1. ❌ يتم رفض التغيير
2. 📝 يتم تسجيل المحاولة في AUDIT_LOG.md
3. ⚠️ يتم تنبيه المستخدم

---

## إحصائيات الحماية الحالية

| الفئة | العدد |
|-------|-------|
| ملفات CRITICAL | 15 |
| ملفات HIGH | 14 |
| ملفات MEDIUM | 20+ |
| Storage Buckets | 4 |
| إجمالي المحمي | 50+ ملف |
| RLS Policies مفحوصة | 708+ |

---

## الملفات ذات الصلة

- `docs/PROTECTED_FILES.md` - قائمة الملفات المحمية
- `docs/AUDIT_LOG.md` - سجل الفحص والتعديلات
