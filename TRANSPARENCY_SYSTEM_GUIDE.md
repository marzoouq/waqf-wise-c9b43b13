# دليل نظام الشفافية والصلاحيات الشامل

## نظرة عامة

تم تطوير نظام صلاحيات شامل يتيح للناظر التحكم الكامل في ما يراه المستفيدون من الدرجة الأولى (14 مستفيد) في بوابة المستفيدين. النظام يحتوي على **50 صلاحية** منظمة في **8 فئات رئيسية**.

---

## 📊 الصلاحيات المتاحة (50 صلاحية)

### 1️⃣ الأقسام الرئيسية (13 صلاحية)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_overview` | لوحة التحكم الرئيسية والإحصائيات | ✅ مُفعّل |
| `show_profile` | البيانات الشخصية والتواصل | ✅ مُفعّل |
| `show_requests` | تقديم ومتابعة الطلبات | ✅ مُفعّل |
| `show_distributions` | سجل توزيعات الغلة | ✅ مُفعّل |
| `show_statements` | كشف حساب مفصل بالحركات | ✅ مُفعّل |
| `show_properties` | معلومات عقارات الوقف | ✅ مُفعّل |
| `show_documents` | رفع وتحميل المستندات | ✅ مُفعّل |
| `show_bank_accounts` | الحسابات البنكية للوقف | ❌ مُعطّل |
| `show_financial_reports` | التقارير المالية المتاحة | ❌ مُعطّل |
| `show_approvals_log` | سجل الموافقات والقرارات | ❌ مُعطّل |
| `show_disclosures` | التقارير السنوية المنشورة | ❌ مُعطّل |
| `show_governance` | معلومات الحوكمة والإدارة | ❌ مُعطّل |
| `show_budgets` | الميزانيات والخطط المالية | ❌ مُعطّل |

---

### 2️⃣ الجداول المالية (8 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_bank_balances` | عرض الأرصدة الحالية للحسابات | ❌ مُعطّل |
| `show_bank_transactions` | جميع المعاملات البنكية | ❌ مُعطّل |
| `show_bank_statements` | كشوف الحساب الشهرية | ❌ مُعطّل |
| `show_invoices` | فواتير الموردين والمقاولين | ❌ مُعطّل |
| `show_contracts_details` | تفاصيل عقود الإيجار والصيانة | ❌ مُعطّل |
| `show_maintenance_costs` | مصاريف صيانة العقارات | ❌ مُعطّل |
| `show_property_revenues` | إيرادات كل عقار | ❌ مُعطّل |
| `show_expenses_breakdown` | تفصيل جميع المصروفات | ❌ مُعطّل |

---

### 3️⃣ المستفيدون الآخرون (8 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_other_beneficiaries_names` | أسماء المستفيدين الآخرين | ❌ مُعطّل |
| `show_other_beneficiaries_amounts` | المبالغ المصروفة للآخرين | ❌ مُعطّل |
| `show_other_beneficiaries_personal_data` | بيانات شخصية عن الآخرين | ❌ مُعطّل |
| `show_family_tree` | شجرة العائلة الكاملة | ✅ مُفعّل |
| `show_total_beneficiaries_count` | إجمالي عدد المستفيدين | ❌ مُعطّل |
| `show_beneficiary_categories` | تصنيفات المستفيدين | ❌ مُعطّل |
| `show_beneficiaries_statistics` | إحصائيات عامة | ❌ مُعطّل |
| `show_inactive_beneficiaries` | المستفيدون المتوقفون | ❌ مُعطّل |

---

### 4️⃣ البيانات الحساسة والإخفاء (5 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `mask_iban` | إخفاء IBAN (عرض آخر 4 أرقام فقط) | ✅ مُفعّل |
| `mask_phone_numbers` | إخفاء جزئي لأرقام الهواتف | ✅ مُفعّل |
| `mask_exact_amounts` | عرض مبالغ تقريبية بدلاً من دقيقة | ❌ مُعطّل |
| `mask_tenant_info` | حماية خصوصية المستأجرين | ✅ مُفعّل |
| `mask_national_ids` | إخفاء جزئي لرقم الهوية | ✅ مُفعّل |

**مثال على الإخفاء:**
- **IBAN**: `SA03 8000 0000 **** **** 1234` (عرض آخر 4 أرقام فقط)
- **الهاتف**: `05XX XXX 1234` (إخفاء الأرقام الوسطى)
- **المبالغ**: `~250,000 ريال` (تقريبي بدلاً من 248,573 ريال)
- **الهوية**: `10XXXXXX34` (عرض أول رقمين وآخر رقمين فقط)

---

### 5️⃣ الحوكمة والقرارات (6 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_governance_meetings` | محاضر اجتماعات مجلس الإدارة | ❌ مُعطّل |
| `show_nazer_decisions` | القرارات الصادرة من الناظر | ❌ مُعطّل |
| `show_policy_changes` | التحديثات على السياسات | ❌ مُعطّل |
| `show_strategic_plans` | خطط الوقف المستقبلية | ❌ مُعطّل |
| `show_audit_reports` | تقارير المراجعة الداخلية | ❌ مُعطّل |
| `show_compliance_reports` | تقارير الامتثال القانوني | ❌ مُعطّل |

---

### 6️⃣ القروض والفزعات (5 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_own_loans` | قروض المستفيد الشخصية | ✅ مُفعّل |
| `show_other_loans` | قروض باقي المستفيدين | ❌ مُعطّل |
| `mask_loan_amounts` | إخفاء القيم الدقيقة للقروض | ❌ مُعطّل |
| `show_emergency_aid` | المساعدات الطارئة (الفزعات) | ✅ مُفعّل |
| `show_emergency_statistics` | إحصائيات عامة للفزعات | ❌ مُعطّل |

---

### 7️⃣ الميزانيات والتخطيط (4 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_annual_budget` | ميزانية الوقف السنوية | ❌ مُعطّل |
| `show_budget_execution` | نسب التنفيذ الفعلية | ❌ مُعطّل |
| `show_reserve_funds` | الاحتياطيات المالية | ❌ مُعطّل |
| `show_investment_plans` | الخطط الاستثمارية للوقف | ❌ مُعطّل |

---

### 8️⃣ المحاسبة والتواصل (5 صلاحيات)

| الصلاحية | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `show_journal_entries` | جميع القيود المحاسبية | ❌ مُعطّل |
| `show_trial_balance` | ميزان المراجعة الشهري | ❌ مُعطّل |
| `show_ledger_details` | دفتر الأستاذ التفصيلي | ❌ مُعطّل |
| `show_internal_messages` | المراسلات مع الإدارة | ✅ مُفعّل |
| `show_support_tickets` | نظام تذاكر الدعم الفني | ❌ مُعطّل |

**إعدادات عامة (2):**
- `allow_export_pdf`: السماح بتصدير PDF (❌ مُعطّل)
- `allow_print`: السماح بالطباعة (❌ مُعطّل)

---

## 🎯 كيفية الوصول لإعدادات الشفافية

### للناظر أو المشرف:
1. افتح **الإعدادات** من القائمة الجانبية
2. اضغط على **إعدادات الشفافية للمستفيدين**
3. ستجد 8 تبويبات لإدارة الصلاحيات

### التبويبات الـ 8:
1. **الأقسام** - التحكم في الأقسام الرئيسية (13)
2. **المالية** - الجداول المالية التفصيلية (8)
3. **المستفيدون** - بيانات المستفيدين الآخرين (8)
4. **الخصوصية** - إعدادات الإخفاء والأمان (7)
5. **الحوكمة** - معلومات الإدارة والقرارات (6)
6. **القروض** - القروض والفزعات (5)
7. **الميزانيات** - الميزانيات والتخطيط (4)
8. **المحاسبة** - المحاسبة التفصيلية والتواصل (5)

---

## 🔐 كيف يعمل نظام الإخفاء

### MaskedValue Component

يستخدم النظام مكون `MaskedValue` لإخفاء البيانات الحساسة بذكاء:

```tsx
<MaskedValue
  value="SA03 8000 0000 6080 1016 7519"
  type="iban"
  masked={settings?.mask_iban || false}
  showToggle={true} // اختياري: زر لإظهار/إخفاء
/>
```

**أنواع البيانات المدعومة:**
- `iban` - أرقام الحسابات الدولية
- `phone` - أرقام الهواتف
- `amount` - المبالغ المالية
- `national_id` - أرقام الهوية

---

## 📱 بوابة المستفيدين

### التبويبات المتاحة (ديناميكية حسب الصلاحيات):

1. **نظرة عامة** - إحصائيات سريعة وإجراءات شائعة
2. **الملف الشخصي** - البيانات الشخصية (مع إخفاء الحساسة)
3. **الطلبات** - تقديم ومتابعة الطلبات
4. **التوزيعات** - سجل التوزيعات المستلمة (مع إخفاء المبالغ)
5. **كشف الحساب** - تفاصيل الحركات المالية
6. **العقارات** - معلومات العقارات (مع إخفاء بيانات المستأجرين)
7. **المستندات** - رفع وتحميل المستندات
8. **العائلة** - شجرة العائلة
9. **الوقف** - ملخص عام عن الوقف
10. **الحوكمة** - معلومات الحوكمة (إن كانت مُفعّلة)
11. **الميزانيات** - الميزانيات والخطط (إن كانت مُفعّلة)
12. **القروض** - القروض والفزعات

---

## 🛠️ الملفات الرئيسية

### Backend (Database):
- `supabase/migrations/..._create_full_visibility_system.sql`
  - جدول `beneficiary_visibility_settings`
  - جدول `beneficiary_visibility_audit`
  - دالة `is_first_class_beneficiary()`
  - دالة `get_beneficiary_visibility_settings()`

### Frontend Hooks:
- `src/hooks/useVisibilitySettings.ts` - Hook لجلب وتحديث الإعدادات
- `src/hooks/useUserRole.ts` - Hook للتحقق من الأدوار

### Components:
- `src/components/shared/MaskedValue.tsx` - مكون الإخفاء الذكي
- `src/pages/TransparencySettings.tsx` - صفحة إعدادات الناظر
- `src/pages/BeneficiaryPortal.tsx` - بوابة المستفيدين الرئيسية

### Beneficiary Components (9 مكونات):
1. `BeneficiaryProfileTab.tsx` - الملف الشخصي
2. `BeneficiaryRequestsTab.tsx` - الطلبات
3. `BeneficiaryDistributionsTab.tsx` - التوزيعات
4. `BeneficiaryStatementsTab.tsx` - كشف الحساب
5. `BeneficiaryPropertiesTab.tsx` - العقارات
6. `BeneficiaryDocumentsTab.tsx` - المستندات
7. `WaqfSummaryTab.tsx` - ملخص الوقف
8. `FamilyTreeTab.tsx` - شجرة العائلة
9. `BankAccountsTab.tsx` - الحسابات البنكية
10. `FinancialReportsTab.tsx` - التقارير المالية
11. `ApprovalsLogTab.tsx` - سجل الموافقات
12. `DisclosuresTab.tsx` - الإفصاحات السنوية
13. `GovernanceTab.tsx` - الحوكمة
14. `BudgetsTab.tsx` - الميزانيات
15. `LoansOverviewTab.tsx` - القروض والفزعات

---

## 📋 سيناريوهات الاستخدام

### سيناريو 1: شفافية كاملة
```sql
-- تفعيل جميع الصلاحيات
UPDATE beneficiary_visibility_settings
SET 
  show_overview = true,
  show_profile = true,
  show_requests = true,
  show_distributions = true,
  show_statements = true,
  show_properties = true,
  show_documents = true,
  show_bank_accounts = true,
  show_financial_reports = true,
  show_approvals_log = true,
  show_disclosures = true,
  show_governance = true,
  show_budgets = true,
  -- ... إلخ (جميع الصلاحيات)
  mask_iban = false,
  mask_phone_numbers = false,
  mask_exact_amounts = false,
  allow_export_pdf = true,
  allow_print = true;
```

### سيناريو 2: خصوصية قصوى
```sql
-- تعطيل معظم الصلاحيات وتفعيل الإخفاء
UPDATE beneficiary_visibility_settings
SET 
  show_overview = true,
  show_profile = true,
  show_requests = true,
  show_distributions = true,
  -- تعطيل باقي الأقسام
  show_statements = false,
  show_properties = false,
  show_bank_accounts = false,
  -- ... إلخ
  mask_iban = true,
  mask_phone_numbers = true,
  mask_exact_amounts = true,
  mask_tenant_info = true,
  mask_national_ids = true,
  allow_export_pdf = false,
  allow_print = false;
```

### سيناريو 3: توازن معقول (الإعداد الافتراضي)
- الأقسام الأساسية: ✅ مُفعّلة
- البيانات المالية التفصيلية: ❌ مُعطّلة
- بيانات المستفيدين الآخرين: ❌ مُعطّلة
- الإخفاء: ✅ مُفعّل للبيانات الحساسة

---

## 🔍 سجل التغييرات (Audit)

كل تغيير في الإعدادات يُسجل في جدول `beneficiary_visibility_audit`:
- من قام بالتغيير؟
- متى؟
- ما القيمة القديمة والجديدة؟
- عنوان IP للتسجيل

---

## ✅ الخلاصة

تم بناء نظام صلاحيات شامل يوفر:
- ✅ **50 صلاحية** دقيقة
- ✅ **8 فئات** منظمة
- ✅ **إخفاء ذكي** للبيانات الحساسة
- ✅ **تحكم كامل** للناظر
- ✅ **سجل تدقيق** شامل
- ✅ **واجهة مستخدم** سهلة ومنظمة
- ✅ **مرونة عالية** في التخصيص

---

## 🚀 الخطوات التالية

1. **اختبار شامل** لجميع الصلاحيات
2. **تدريب المستخدمين** على النظام الجديد
3. **مراجعة دورية** للإعدادات الافتراضية
4. **إضافة صلاحيات جديدة** حسب الحاجة
5. **تحسينات مستمرة** بناءً على ملاحظات المستخدمين

---

**تاريخ التحديث:** 24 نوفمبر 2024  
**الإصدار:** 1.0.0  
**حالة النظام:** ✅ مكتمل وجاهز للاستخدام
